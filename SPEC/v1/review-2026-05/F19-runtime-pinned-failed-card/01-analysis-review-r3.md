## Analysis review

1. Add the missing executor-result status writer to the runtime-originating `CardStatus` inventory. [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L725-L733) directly writes `status: execResult.status` from the runtime layer, but r3's inventory and Step 5 checklist skip it because the grep only catches single-line `cardStore.update(...status...)` calls. Classify this site as in-scope for `transitionCard`, then update the evidence-registration analysis because [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L740) currently runs after the card may already have been written to `done` by the omitted site.

2. Correct the L266 startup-repair classification before using it as implementation guidance. [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L266) runs when `active_card_run.phase === 'reviewer'` and the card has no persisted review; the current code does not prove the card is in `RESTARTABLE_STATES`, and `activateGoal` can leave a goal in `active` ([src/cards/card-store.ts](../../../../src/cards/card-store.ts#L1097-L1105)). The r3 plan's claim that reviewer-phase repair cards are all non-`active`/`running` by construction is unsupported; either prove that invariant from runtime-state construction or pick a machine action that legally handles `active`/`running` reviewer repair.

## Design review

1. Make the `RuntimeStateMachine.transitionCard` contract compose with F13 r3's binding async store surface. F13 r3 says `Runtime.open` / `ActiveRuntime.open` replace constructors and that post-F13 status writes become awaited `cardStore.setStatus(...)` calls ([../F13-canonical-index-drift/03-plan-r3.md](../F13-canonical-index-drift/03-plan-r3.md#L57-L61), [../F13-canonical-index-drift/03-plan-r3.md](../F13-canonical-index-drift/03-plan-r3.md#L234-L238)). F19 r3 snippets call `transitionCard(...)` synchronously throughout Step 5. Specify whether `transitionCard` is async and require every runtime call site to `await` it, or explicitly justify how it can remain synchronous after F13 without bypassing the new card-store persistence/audit path.

2. Fix the action-to-transition table so every emitted sequence is legal under `VALID_TRANSITIONS`, since the design says every one-step transition still goes through the store validator. `restart` cannot decompose `cancelled -> backlog -> active -> running` because `cancelled` only allows `drafting` in [src/cards/card-store.ts](../../../../src/cards/card-store.ts#L217-L227). `fail` cannot be a one-step `any non-terminal -> failed` action because only `running -> failed` is valid in that same table. Spell out legal sequences for `cancelled` restart, reviewer-interrupted resume, planner failure from an `active` goal, executor failure, and evidence-registration failure after a nominal `done` result, then make the tests assert the emitted sequences, not just accept/reject booleans.

3. Align `enforceInvariants` staging between the design and plan. The design says Step 4 flips `enforceInvariants` to `true` and I1-I3 auto-correction lights up, but the plan says the corrective bodies do not land until Step 6 and Step 4 still reaches only I4 plus observe-only logging. Keep the required lifecycle `Step 3 false -> Step 4 true -> Step 7 removed`, but make Step 4 actually include the corrective code or state that auto-correction remains inactive until Step 6 and adjust the Step 4 tests/live expectations accordingly.

## Plan review

1. Expand Step 5 to convert [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L725-L733) and restructure executor terminal handling around the machine. The plan should route executor `done` and `failed` outcomes through `transitionCard` before non-status payload updates, and it should avoid the current `done` write followed by a `done -> failed` evidence-registration downgrade unless the design adds a legal machine sequence for that case. Add tests that cover executor `done`, executor `failed`, and `done` with evidence-registration failure.

2. Replace the final status-writer grep gate with one that is both complete and correctly scoped. The proposed `rg -n "cardStore\.setStatus|cardStore\.update\([^\n]*status|untrackedChanges\.status" src/runtime/runtime.ts` misses the multi-line top-level status write at [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L725-L733), while it falsely matches nested result payload fields such as `planning.status` at [src/runtime/runtime.ts](../../../../src/runtime/runtime.ts#L644-L663). Use an AST/scripted gate or a multiline pattern that detects top-level `status` in `cardStore.update` patches, allows nested result metadata, and remains scoped to `src/runtime/runtime.ts` so analyst/operator surfaces stay explicitly out of scope.

3. Add transition-sequence tests for all `RESTARTABLE_STATES` and relevant fail paths, not only the failed-card happy sequence. The current plan tests the full `failed -> backlog -> active -> running` sequence but only accept/rejects `done`, `cancelled`, `blocked`, and `changed`; that would not catch the invalid `cancelled -> backlog` sequence or illegal one-step fail transitions. Include sequence assertions against `STARTABLE_STATES` and `RESTARTABLE_STATES` from [src/permissions/card-permissions.ts](../../../../src/permissions/card-permissions.ts#L28-L29) and `VALID_TRANSITIONS` from [src/cards/card-store.ts](../../../../src/cards/card-store.ts#L217-L227).

VERDICT: CHANGES_REQUESTED
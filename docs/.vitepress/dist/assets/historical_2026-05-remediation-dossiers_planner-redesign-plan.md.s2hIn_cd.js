import{_ as s,o as i,c as a,ae as t}from"./chunks/framework.B2ul6ABY.js";const k=JSON.parse('{"title":"Planner Redesign Implementation Plan","description":"","frontmatter":{},"headers":[],"relativePath":"historical/2026-05-remediation-dossiers/planner-redesign-plan.md","filePath":"historical/2026-05-remediation-dossiers/planner-redesign-plan.md"}'),n={name:"historical/2026-05-remediation-dossiers/planner-redesign-plan.md"};function d(r,e,o,l,c,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="planner-redesign-implementation-plan" tabindex="-1">Planner Redesign Implementation Plan <a class="header-anchor" href="#planner-redesign-implementation-plan" aria-label="Permalink to &quot;Planner Redesign Implementation Plan&quot;">​</a></h1><p>This is the implementation plan for the Saivage v3 planner redesign. The central rule is simple: a planner does not call another planner or executor directly. A planner creates or edits cards, then asks the runtime to <code>activate_card(card_id)</code>. The runtime decides how to run that card, owns the planner/reviewer cycle for goal cards, and returns one terminal result to the caller.</p><p><code>activate_card</code> is named for the control-transfer semantics: the same planner session that owns a goal card may receive control again on a subsequent <code>activate_card</code> call from its parent. Executor sessions are one-shot per activation; a re-activation of a terminal card always opens a new executor session. It is not a one-shot &quot;run&quot;.</p><p>No backward compatibility is required. Existing <code>.saivage/</code> state, session logs, RuntimeState files, card result shapes, tests, and HTTP API shapes from previous Saivage v3 invocations are discarded or rewritten. A clean architecture is the priority.</p><h2 id="_1-design-rules" tabindex="-1">1. Design Rules <a class="header-anchor" href="#_1-design-rules" aria-label="Permalink to &quot;1. Design Rules&quot;">​</a></h2><ul><li><strong>Cards are the unit of work.</strong> A card is activated through the runtime tool <code>activate_card(card_id)</code>. The parent planner does not know or care whether the child is handled by an executor, a planner, a reviewer, or a retry loop.</li><li><strong><code>activate_card</code> is terminal at the parent boundary.</strong> It returns only <code>done</code>, <code>failed</code>, or <code>blocked</code>. <code>needs_corrections</code> is an internal reviewer verdict handled inside the active activation.</li><li><strong>Single active planner.</strong> At most one planner session is <code>Running</code> globally. Ancestor planners are <code>AwaitingChild</code> by virtue of holding an unresolved <code>activate_card</code> tool call; the runtime does not persist an ancestor chain separately.</li><li><strong>Idle bootstrap.</strong> The runtime starts idle. The analyst calls <code>lets_dance</code> to record a kickoff directive on the project card; the runtime itself calls <code>activate_card(projectCardId)</code> on its next safe tick. There is no other project bootstrap path.</li><li><strong>Reviewer is runtime-owned and gated.</strong> The planner signals completion with <code>report_goal_done</code>; the runtime first checks subtree readiness (§3.3) and evidence (§3.2), then invokes the reviewer, applies the verdict, and either finishes the card or resumes the same planner inside the same activation until the retry limit is reached.</li><li><strong>Status flows only at terminal report.</strong> Every executor terminal response and every <code>report_goal_*</code> call carries a <code>status_text</code>. The runtime mirrors it onto the card. There is no <code>set_status_text</code> tool and no mid-run status update obligation.</li><li><strong>Reviewer results live on the card.</strong> A <code>failed</code> activation does not duplicate reviewer issues into the <code>tool_result</code>; the parent reads them from <code>card.result.review</code> on the failed child card.</li><li><strong>Evidence and subtree-readiness failures are tool errors, not reviewer corrections.</strong> <code>report_goal_done</code> rejects with explicit <code>subtree_not_ready</code> / <code>invalid_evidence</code> tool errors that do not consume <code>max_review_retries</code>.</li><li><strong>No notification acknowledgement gate.</strong> Notifications never block <code>report_goal_done</code> and never wake a planner.</li><li><strong>Parentage comes from the card tree.</strong> Use <code>CardStore.getParent(card_id)</code> for hierarchy. <code>AgentSession</code> does not store <code>parent_session_id</code> or <code>parent_tool_call_id</code>.</li><li><strong>Transient caller edges are runtime state.</strong> The active caller edge for the in-flight leaf card-run lives in <code>RuntimeState.active_card_run</code>, not on the child session.</li><li><strong>Async shape is only for shell processes.</strong> The asynchronous primitive exposed to agents is the durable shell-process layer: synchronous <code>start_and_wait</code> / <code>run_project_command</code> plus <code>wait_for_process</code> and <code>kill_process</code> over persisted <code>ProcessRecord</code>s.</li><li><strong>Analyst mutates state through pause → mutate → unpause.</strong> The active planner sees changes via a synthetic note appended on resume.</li><li><strong>Clean-slate boot.</strong> If the new runtime finds a <code>.saivage/</code> directory created by old v3 code, it moves it aside to <code>.saivage.discarded-&lt;timestamp&gt;/</code> and starts from an empty state.</li></ul><h2 id="_2-scope" tabindex="-1">2. Scope <a class="header-anchor" href="#_2-scope" aria-label="Permalink to &quot;2. Scope&quot;">​</a></h2><h3 id="remove" tabindex="-1">Remove <a class="header-anchor" href="#remove" aria-label="Permalink to &quot;Remove&quot;">​</a></h3><ul><li><code>src/utils/planner-control.ts</code>, <code>PlannerControlFrame</code>, <code>DispatchRecord</code>, dispatch queue persistence, and old frame files under <code>.saivage/runtime/</code>.</li><li><code>Runtime.dispatchGoal</code>, <code>Runtime.applyPlannerResult</code>, <code>Runtime.executeReadyCards</code>, <code>_dispatchReadyCards</code>, <code>_checkContinuousImprovement</code>, and any <code>MAX_ITERATIONS</code> dispatch loop.</li><li><code>PlannerResult</code> and <code>PlanningResult</code> as the planner output and Goal-Context types. Replaced by <code>LatestSelfReport</code> (§8).</li><li><code>AgentRuntime.invokePlanner(goalId, prompt)</code> and old per-step planner execution APIs.</li><li>Planner tools named <code>start_planner</code>, <code>start_executor</code>, any intermediate name <code>run_card</code> from earlier drafts, and <code>set_status_text</code>.</li><li><code>resumePlannerSession</code>, <code>PlannerResumeDirective</code>, and runtime paths that resume non-project planners outside an activation.</li><li><code>RuntimeState.current_card_id</code>, <code>current_agent_session_id</code>, dispatch queue fields, <code>active_planner_sessions</code>, and <code>reviewer_in_flight</code>.</li><li>Session fields <code>parent_session_id</code> and <code>parent_tool_call_id</code> from <code>AgentSession</code>.</li><li>Session status values inherited from legacy state, including <code>active</code>, <code>done</code>, <code>failed</code>, and <code>awaiting_review</code>.</li><li>The <code>unacknowledged_notification</code> gate on <code>report_goal_done</code>, the <code>Suspended</code> session lifecycle state, and any <code>acknowledge_notification</code> planner tool.</li><li>The <code>running-blocked</code> card status (replaced by <code>changed</code>, §5).</li><li>The legacy runtime dispatch endpoint (<code>/api/runtime/dispatch</code>) and tests that assert on the old dispatch loop.</li><li>Any test that expects <code>needs_corrections</code> as a parent-visible activation result, <code>start_planner</code>/<code>start_executor</code>/<code>run_card</code> tool names, session parent fields, Tier-A/Tier-B correction propagation, runtime-driven resumption of a non-project planner, or a notification acknowledgement gate.</li></ul><h3 id="add-or-rewrite" tabindex="-1">Add or Rewrite <a class="header-anchor" href="#add-or-rewrite" aria-label="Permalink to &quot;Add or Rewrite&quot;">​</a></h3><ul><li>Planner tool <code>activate_card(card_id) -&gt; CardActivationOutcome</code>.</li><li><code>report_goal_done / _failed / _blocked</code> carrying a required <code>status_text</code> field; runtime mirrors it onto the card.</li><li>Executor terminal response shape carrying a required <code>status_text</code> field; runtime mirrors it onto the card.</li><li>Analyst tool <code>lets_dance()</code> and HTTP <code>POST /api/runtime/lets_dance</code>.</li><li><code>AgentRuntime.activateCard(cardId, caller)</code> as the runtime umbrella method for card activation.</li><li><code>AgentRuntime.ensurePlannerSession(goalId)</code> as an idempotent helper used by <code>activateCard</code> to create or resume the planner for a goal card.</li><li><code>AgentRuntime.invokeExecutor(cardId, goalId, prompt)</code> for terminal card execution.</li><li><code>AgentRuntime.invokeReviewer(goalId, assessmentId, prompt)</code> for the runtime-owned review step.</li><li>Runtime safe-tick loop that, when idle and unpaused, consumes a pending <code>lets_dance</code> or project-correction directive by calling <code>activate_card(projectCardId)</code> itself.</li><li>Runtime acceptance gates on <code>report_goal_done</code>: <code>subtree_not_ready</code>, then <code>invalid_evidence</code>, then reviewer.</li><li><code>recordReviewerVerdict</code>, <code>addPendingCorrectionNote</code>, and card-side review/correction persistence.</li><li>Card-side <code>status_text</code>, <code>status_text_updated_at</code>, and <code>status_text_author_session_id</code> fields written by the runtime from terminal agent responses.</li><li><code>RuntimeState.active_card_run</code>, the single leaf card-run record (or <code>null</code> when idle).</li><li><code>ProcessRecord</code> parent/caller fields and synchronous process tools (<code>start_and_wait</code>, <code>run_project_command</code>) are in scope. Durable async process handling (<code>wait_for_process</code>, <code>kill_process</code>, persistent <code>ProcessRecord</code>s with <code>command_hash</code>, <code>cwd_canonical</code>, <code>started_at_monotonic</code>, and process reattach across restart) is a future stage (§14).</li><li>Card status <code>changed</code> (replaces <code>running-blocked</code>) and <code>subtree_changed</code> note kind injected into Goal Context.</li><li>Synthetic-note injection after pause→mutate→unpause so the active planner sees analyst changes on its next turn.</li><li>Planner prompt text that says planners create child cards, call <code>activate_card</code>, and include <code>status_text</code> in every terminal report.</li><li>A basic Goal Context schema surfaced on planner creation and every runtime resume, with <code>status_text</code> mirrored on every child node, <code>latest_self_report</code>, and an explicit <code>resume_reason</code> that includes <code>subtree_changed</code>.</li><li>Tests around activation, internal review retries, retry exhaustion, status-text propagation, subtree-readiness and evidence tool errors, <code>lets_dance</code> bootstrap, analyst pause-mutate-unpause, clean-state boot, and restart repair.</li></ul><h2 id="_3-runtime-tool-contract" tabindex="-1">3. Runtime Tool Contract <a class="header-anchor" href="#_3-runtime-tool-contract" aria-label="Permalink to &quot;3. Runtime Tool Contract&quot;">​</a></h2><p>Planner sessions get one runtime dispatch tool:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardActivationOutcome</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;done&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      evidence_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      failure_kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;executor_failed&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner_failed&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;review_retries_exhausted&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner_force_cancelled&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;service_restart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      blocked_reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">activate_card</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(card_id: string): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CardActivationOutcome</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>The tool is subtree-scoped: the caller can only activate cards in its goal subtree. The card must already exist. The parent planner creates cards with <code>create_card</code>, then activates them with <code>activate_card</code>.</p><p>For terminal cards, the runtime runs one executor session to completion, persists artifacts and the executor result, updates the card (including <code>status_text</code> from the executor&#39;s terminal response), and returns a terminal <code>CardActivationOutcome</code>.</p><p>For goal cards, the runtime runs the goal planner and reviewer loop to a terminal state. The parent planner never sees reviewer-internal <code>needs_corrections</code> verdicts; on retry exhaustion it sees <code>failed</code> and reads issues from <code>card.result.review</code>.</p><p>Re-activating the same <code>Dormant</code> planner is a normal operation: the parent adds a note (typically with <code>add_note</code>) explaining what to correct and calls <code>activate_card(card_id)</code> again. The runtime resumes the same planner with the new Goal Context.</p><p>Completion tools (every variant carries <code>status_text</code> for the runtime to mirror onto the goal card):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">report_goal_done</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  summary: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  status_text: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  evidence_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> string[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">report_goal_failed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  summary: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  status_text: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">report_goal_blocked</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  summary: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  status_text: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  blocked_reason: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Executor terminal response includes the same <code>status_text</code> field alongside the executor result payload.</p><h3 id="_3-1-destructive-card-operations" tabindex="-1">3.1 Destructive Card Operations <a class="header-anchor" href="#_3-1-destructive-card-operations" aria-label="Permalink to &quot;3.1 Destructive Card Operations&quot;">​</a></h3><p>The planner has three card-structural tools beyond <code>create_card</code> and <code>edit_card</code>:</p><ul><li><code>cancel_card(card_id)</code> transitions a card from <code>backlog</code>, <code>active</code>, or <code>changed</code> to <code>cancelled</code>. Refused for the active leaf, for any card whose subtree contains the active leaf, for cards in status <code>running</code>, and for cards already in a terminal status (<code>done</code>/<code>failed</code>/<code>blocked</code>/<code>cancelled</code>). Cancellation cascading onto running ancestors of the active leaf is a future stage (§14). Persists a synthetic <code>latest_self_report</code> of result <code>failed</code> with reason <code>cancelled</code> when none exists. Emits <code>card_cancelled</code>. This is the prerequisite to <code>delete_card</code> for non-terminal cards.</li><li><code>delete_card(card_id)</code> is refused if the target or any descendant equals <code>active_card_run?.card_id</code> or has a non-<code>Dormant</code> planner. Allowed for <code>backlog</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, or <code>cancelled</code> cards. To delete an <code>active</code> or <code>changed</code> card, the caller must first call <code>cancel_card</code>. Deleting a <code>running</code> card is not allowed in this stage (see §14). Archives <code>(fields, notes, result, evidence references)</code> under <code>.saivage/archive/cards/&lt;card_id&gt;.json</code>, removes the card from <code>CardStore</code>, and emits <code>card_destructive_delete</code>. The cascade is all-or-nothing: any failing precondition aborts the operation.</li><li><code>restart_card(card_id)</code> transitions a card from <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code>, or <code>changed</code> back to <code>active</code>. Refused while the card is the active leaf. For terminal cards it clears <code>result.executor</code> (the next executor run will overwrite <code>status_text</code>). For goal cards it clears <code>result.review</code>, clears <code>latest_self_report</code>, and resets <code>correction_attempts</code>; it does NOT wake the planner. The parent must follow with <code>activate_card(card_id)</code> to give control back. Reset of a planner&#39;s LLM message log is deferred (see §14).</li></ul><h3 id="_3-2-evidence-tool-error" tabindex="-1">3.2 Evidence Tool Error <a class="header-anchor" href="#_3-2-evidence-tool-error" aria-label="Permalink to &quot;3.2 Evidence Tool Error&quot;">​</a></h3><p><code>report_goal_done</code> validates evidence before the reviewer phase.</p><ul><li>If evidence validation fails, the call returns a <code>tool_error</code> of kind <code>invalid_evidence</code> with per-card breakdown. The planner stays <code>Running</code>; no card activation outcome is emitted.</li><li>The error does not consume <code>max_review_retries</code>.</li><li>A runtime event <code>goal_report_rejected { kind: &#39;invalid_evidence&#39; }</code> is emitted.</li><li><code>active_card_run</code> is not cleared and no reviewer is invoked.</li><li><strong>A rejected <code>report_goal_*</code> call does NOT mirror its <code>status_text</code> onto the card and does NOT update <code>latest_self_report</code>.</strong> Those fields are written only when a <code>report_goal_*</code> call is accepted.</li><li>Repeated <code>invalid_evidence</code> errors are governed only by the standard stuck-supervisor path; there is no separate cap.</li></ul><h3 id="_3-3-subtree-readiness-tool-error" tabindex="-1">3.3 Subtree-Readiness Tool Error <a class="header-anchor" href="#_3-3-subtree-readiness-tool-error" aria-label="Permalink to &quot;3.3 Subtree-Readiness Tool Error&quot;">​</a></h3><p>Before evidence validation, <code>report_goal_done</code> runs the subtree- readiness gate.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SubtreeReadinessReason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;descendant_blocking&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;changed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span></code></pre></div><p>The runtime rejects <code>report_goal_done</code> with a <code>tool_error</code> of kind <code>subtree_not_ready</code> and payload <code>{ reasons: SubtreeReadinessReason[] }</code> when any descendant card is in status <code>blocked</code> or <code>changed</code> (the configurable &quot;not-safe-to-close&quot; set, extendable in future stages).</p><p>The gate intentionally does not scan descendants for live subprocesses: a subgoal cannot have reported <code>done</code> while it held live processes, because its own readiness gate would have rejected the report. The complementary <code>pending_subprocess</code> reason (for the goal card&#39;s own processes) lands together with the durable async-process stage (§14).</p><p>On rejection:</p><ul><li>The runtime appends a synthetic note on the goal card naming the offending descendants.</li><li>The planner stays <code>Running</code>; <code>active_card_run</code> is not cleared; <code>correction_attempts</code> is not incremented.</li><li>Event <code>goal_report_rejected { kind: &#39;subtree_not_ready&#39; }</code> is emitted.</li></ul><p>Gate order on <code>report_goal_done</code> is fixed:</p><ol><li><code>subtree_not_ready</code> (§3.3).</li><li><code>invalid_evidence</code> (§3.2).</li><li>Reviewer phase (§7.4).</li></ol><h2 id="_4-card-run-state" tabindex="-1">4. Card Run State <a class="header-anchor" href="#_4-card-run-state" aria-label="Permalink to &quot;4. Card Run State&quot;">​</a></h2><p><code>RuntimeState</code> stores only the single leaf card-run; the ancestor chain is derived on demand from <code>CardStore.getParent</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RuntimeState</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  paused</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  notice</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_card_run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  runtime_status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;running&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;force_cancelled&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  phase</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;executor&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  caller_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  caller_tool_call_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  planner_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  executor_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  reviewer_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  correction_attempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  started_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  last_turn_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Invariants:</p><ul><li><code>active_card_run</code> is <code>null</code> when the runtime is idle; otherwise it points at the single card-run currently doing work.</li><li>An ancestor planner&#39;s <code>AwaitingChild</code> status is implied by its having an unresolved <code>activate_card</code> tool call in its message log; no separate field is persisted.</li><li>Each card has at most one activation in flight at any moment. A planner session may have at most one unresolved <code>activate_card</code> tool call at any time and may not wait on a process and an <code>activate_card</code> simultaneously. Synchronous shell tools (<code>start_and_wait</code>, <code>run_project_command</code>) complete within an LLM turn, so this stage has no concept of a long-lived process wait that survives across <code>activate_card</code>; that lands with the durable async-process stage (§14).</li><li>Caller-edge reconstruction: when the active leaf terminates, the runtime computes <code>parentCardId = CardStore.getParent(leaf.card_id)</code>, looks up planner session <code>planner:&lt;parentCardId&gt;</code> (which must be <code>AwaitingChild</code>), finds the unique unresolved <code>activate_card(leaf.card_id)</code> tool call in its message log, and uses that session id + tool_call_id as the caller edge. The parent&#39;s <code>ActiveCardRun</code> is reconstructed with <code>phase: &#39;planner&#39;</code> and the synthesized <code>CardActivationOutcome</code> is delivered as the parent&#39;s next <code>tool_result</code>. Unwinding repeats recursively. No persisted caller stack is required; restart repair uses the same algorithm.</li><li>System-caller path: if <code>parentCardId === null</code> the leaf is the project card. The runtime persists the outcome on the project card (status, <code>result.review</code> when applicable, <code>latest_self_report</code>), clears <code>active_card_run</code> to <code>null</code>, emits <code>project_run_completed</code> (§13), and returns to idle. The specific operator-notification flow after a project completion is a future stage (§14).</li><li><code>ForceCancelled</code> applies to the leaf. Force-cancel emits exactly one synthetic <code>failed</code> tool_result to the leaf&#39;s parent and clears <code>active_card_run</code>; the parent becomes <code>Running</code> again on its next turn. If the cancelled leaf is the project card, the runtime persists a synthetic <code>failed</code> outcome on the project card and emits <code>project_run_completed</code> (no tool_result is delivered).</li><li>Runtime pause is a pure global gate (<code>RuntimeState.paused</code>); it does not change <code>active_card_run</code> or any session lifecycle state. In-flight tool dispatch finishes synchronously, then the runtime stops scheduling new LLM turns. There is no buffered-result schema in this stage; pause-time buffering of durable process results lands with §14.</li></ul><p><code>caller_session_id</code> and <code>caller_tool_call_id</code> are transient runtime edges used to resolve the parent planner&#39;s outstanding <code>activate_card</code> tool_call. They are not written into <code>AgentSession</code>.</p><p>The Web UI renders an active breadcrumb computed server-side by walking <code>CardStore.getParent</code> from <code>active_card_run.card_id</code> up to the project card; the breadcrumb is not persisted.</p><h2 id="_5-card-status-and-status-reporting" tabindex="-1">5. Card Status and Status Reporting <a class="header-anchor" href="#_5-card-status-and-status-reporting" aria-label="Permalink to &quot;5. Card Status and Status Reporting&quot;">​</a></h2><p>Card statuses:</p><ul><li><code>backlog</code>, <code>active</code>, <code>running</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code>.</li><li><code>changed</code>: the card&#39;s state was externally modified (by the analyst or by a descendant subtree correction) since its planner last saw it. The card is NOT immediately re-activated. The parent planner (and, recursively, any ancestor) sees a <code>subtree_changed</code> note in its next Goal Context and decides whether to call <code>activate_card(card_id)</code> on the affected descendant. The reactivation is eventual.</li></ul><p><code>changed</code> replaces the old <code>running-blocked</code> status and applies broadly: it can land on the analyst-flagged origin goal, on a descendant edited under pause, or on any card whose stored state diverges from the planner&#39;s last self-report.</p><p>Status transitions driven by <code>activate_card</code>:</p><table tabindex="0"><thead><tr><th>Source status</th><th>Effect of <code>activate_card(card_id)</code></th></tr></thead><tbody><tr><td><code>backlog</code>, <code>active</code></td><td>Card transitions to <code>running</code>.</td></tr><tr><td><code>changed</code></td><td>Card transitions to <code>running</code>. The <code>changed</code> marker is consumed; any queued <code>subtree_changed</code> notes referencing this card are delivered once and then discarded (§10).</td></tr><tr><td><code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code></td><td>Re-activation is allowed for goal cards as the normal correction path (the <code>Dormant</code> planner is resumed with fresh notes); the card transitions back to <code>running</code>. For terminal cards, <code>restart_card</code> is required first to clear <code>result.executor</code>; otherwise <code>activate_card</code> returns a <code>tool_error</code> of kind <code>terminal_card_requires_restart</code>.</td></tr><tr><td><code>running</code></td><td>Tool error of kind <code>card_already_active</code>: only one activation may be in flight per card.</td></tr></tbody></table><p>The terminal outcome (<code>done</code>, <code>failed</code>, or <code>blocked</code>) replaces <code>running</code> when control returns to the parent.</p><p>Every card carries:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardStatusText</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// free-form, short; null until first accepted report</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text_updated_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ISO timestamp</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text_author_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// writer session id</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>status_text</code> is written by the runtime, never by an agent tool. Sources:</p><ul><li>Executor terminal response includes <code>status_text</code>; the runtime mirrors it onto the card when persisting the executor result.</li><li><code>report_goal_done / _failed / _blocked</code> includes <code>status_text</code>; the runtime mirrors it onto the goal card.</li><li><code>restart_card</code> does not touch <code>status_text</code> on goal cards; for terminal cards the next executor run overwrites it.</li></ul><p>There is no <code>set_status_text</code> tool. Mid-run progress is not surfaced; the operator and ancestor planners see the most recent terminal <code>status_text</code>.</p><p>Ancestors observe descendant progress through <code>child_card_tree</code> inside Goal Context, never by writing.</p><h2 id="_6-agent-sessions" tabindex="-1">6. Agent Sessions <a class="header-anchor" href="#_6-agent-sessions" aria-label="Permalink to &quot;6. Agent Sessions&quot;">​</a></h2><p>Planner session ids are deterministic:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>planner:&lt;goal_card_id&gt;</span></span></code></pre></div><p>Executor session ids include the caller tool call because executor work is one-shot and tied to a specific activation:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>executor:&lt;card_id&gt;:&lt;caller_tool_call_id&gt;</span></span></code></pre></div><p>Reviewer session ids include a preallocated assessment id:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>reviewer:&lt;goal_card_id&gt;:&lt;assessment_id&gt;</span></span></code></pre></div><p>The runtime allocates <code>assessment_id</code> before invoking the reviewer so the reviewer session and persisted assessment share a stable identity.</p><p><code>AgentSession</code> stores the session&#39;s own identity and lifecycle only. It does not store <code>parent_session_id</code> or <code>parent_tool_call_id</code>. The card hierarchy gives durable parentage; <code>active_card_run</code> and <code>ProcessRecord</code> give transient runtime caller edges.</p><p>Planner lifecycle states:</p><ul><li><code>Running</code>: the only planner currently receiving LLM turns.</li><li><code>AwaitingChild</code>: the planner owns an outstanding <code>activate_card</code> or process wait and consumes no LLM turns.</li><li><code>Dormant</code>: the planner has emitted <code>report_goal_done</code>, <code>report_goal_failed</code>, or <code>report_goal_blocked</code> and is waiting for a future activation of the same goal.</li><li><code>ForceCancelled</code>: supervisor or operator stopped the session.</li></ul><p>Runtime pause is global (<code>RuntimeState.paused</code>) and is not a per-session state. There is no <code>Suspended</code> state.</p><p>There is no <code>AwaitingReview</code>. Reviewer progress is represented by <code>active_card_run.phase === &#39;reviewer&#39;</code> and by the goal card&#39;s <code>result.review</code> once persisted.</p><h2 id="_7-runtime-algorithms" tabindex="-1">7. Runtime Algorithms <a class="header-anchor" href="#_7-runtime-algorithms" aria-label="Permalink to &quot;7. Runtime Algorithms&quot;">​</a></h2><h3 id="_7-1-startup-and-idle-loop" tabindex="-1">7.1 Startup and Idle Loop <a class="header-anchor" href="#_7-1-startup-and-idle-loop" aria-label="Permalink to &quot;7.1 Startup and Idle Loop&quot;">​</a></h3><ol><li>Load configuration and check the target project&#39;s <code>.saivage/</code> layout.</li><li>If the directory is from an older v3 schema, move it to <code>.saivage.discarded-&lt;timestamp&gt;/</code> and initialize a clean runtime.</li><li>Load CardStore, NotificationCenter, ProviderRegistry, AgentAdapter, process-runner registry, and StuckSupervisor.</li><li>Reconcile persisted process records. Only synchronous shell commands are exposed in this stage; durable async processes, restart identity probes, and pause-time buffering of process terminal results land with §14.</li><li>Reconcile orphan planner tool calls. A planner message log with an <code>activate_card</code> call and no matching result is repaired from <code>active_card_run</code>, the child card&#39;s terminal status, or a synthetic <code>service_restart</code> failure. If <code>active_card_run</code> is in <code>phase: &#39;reviewer&#39;</code> with no persisted <code>result.review</code>, follow the reviewer-interrupt recovery path (§7.4): transition the goal&#39;s planner session back to <code>Running</code>, set <code>phase: &#39;planner&#39;</code>, and queue a <code>reviewer_interrupted</code> note in Goal Context.</li><li>Enter the idle loop. The runtime is idle when <code>active_card_run === null</code> and not paused. On every safe tick, the runtime checks for a pending <code>lets_dance</code> or project-correction directive on the project card; if one is present and no in-flight startup repair remains, the runtime calls <code>activate_card(projectCardId)</code> itself.</li><li>Start REST, WebSocket, and supervisor surfaces.</li></ol><p>The runtime never enumerates goal cards at startup to spawn their planners. Every planner, including the project planner, is created or resumed only through <code>activate_card</code>.</p><h3 id="_7-2-activating-a-terminal-card" tabindex="-1">7.2 Activating a Terminal Card <a class="header-anchor" href="#_7-2-activating-a-terminal-card" aria-label="Permalink to &quot;7.2 Activating a Terminal Card&quot;">​</a></h3><ol><li>Assert the card is inside the caller&#39;s subtree and is a terminal card.</li><li>Set <code>active_card_run</code> to an <code>ActiveCardRun</code> for <code>card_id</code> with <code>phase: &#39;executor&#39;</code> and the caller edge.</li><li>Create or reattach <code>executor:&lt;card_id&gt;:&lt;caller_tool_call_id&gt;</code>.</li><li>Run the executor to completion. The executor&#39;s terminal response carries <code>status_text</code>; the runtime mirrors it onto the card.</li><li>Persist artifacts, attachments, and <code>card.result.executor</code>.</li><li>Mark the card <code>done</code>, <code>failed</code>, or <code>blocked</code> according to the executor result.</li><li>Walk one step up via <code>CardStore.getParent</code>, reconstruct the parent&#39;s <code>ActiveCardRun</code>, and resolve the parent&#39;s <code>activate_card</code> tool call with a terminal outcome. If no parent exists (the leaf was the project card), set <code>active_card_run = null</code>.</li></ol><h3 id="_7-3-activating-a-goal-card" tabindex="-1">7.3 Activating a Goal Card <a class="header-anchor" href="#_7-3-activating-a-goal-card" aria-label="Permalink to &quot;7.3 Activating a Goal Card&quot;">​</a></h3><ol><li>Assert the goal is inside the caller&#39;s subtree.</li><li>Set <code>active_card_run</code> to an <code>ActiveCardRun</code> for <code>goalId</code> with <code>phase: &#39;planner&#39;</code> and the caller edge.</li><li>Call <code>ensurePlannerSession(goalId)</code>. If the session exists as <code>Dormant</code>, transition it to <code>Running</code> and inject the freshly rebuilt Goal Context (with <code>resume_reason</code>) as a synthetic user turn.</li><li>Drive the planner until it reports done, failed, or blocked. The planner&#39;s terminal report carries <code>status_text</code>; the runtime mirrors it onto the goal card.</li><li>On <code>report_goal_failed</code>, mark the goal <code>failed</code>, persist <code>latest_self_report</code>, mark the planner <code>Dormant</code>, unwind to the parent.</li><li>On <code>report_goal_blocked</code>, mark the goal <code>blocked</code>, persist <code>latest_self_report</code>, mark the planner <code>Dormant</code>, unwind to the parent.</li><li>On <code>report_goal_done</code>, run §7.4. Subtree-readiness and evidence validation happen there as tool errors that bounce back into the planner without clearing <code>active_card_run</code>.</li></ol><h3 id="_7-4-acceptance-gates-and-reviewer-phase" tabindex="-1">7.4 Acceptance Gates and Reviewer Phase <a class="header-anchor" href="#_7-4-acceptance-gates-and-reviewer-phase" aria-label="Permalink to &quot;7.4 Acceptance Gates and Reviewer Phase&quot;">​</a></h3><p>Inside the active activation, in this fixed order:</p><ol><li><strong>Subtree readiness.</strong> Walk descendants of the goal. If any is in status <code>blocked</code> or <code>changed</code>, return <code>report_goal_done</code> as a <code>tool_error</code> of kind <code>subtree_not_ready</code> with <code>reasons</code> per §3.3. Append a synthetic note on the goal card listing the offending descendants. Do not clear <code>active_card_run</code>. Do not increment <code>correction_attempts</code>. (The <code>pending_subprocess</code> reason lands with the durable async-process stage, §14.)</li><li><strong>Evidence.</strong> Every evidence card must be a descendant of the goal card and must carry durable evidence per §8. On failure, return <code>report_goal_done</code> as a <code>tool_error</code> of kind <code>invalid_evidence</code> per §3.2. Do not clear <code>active_card_run</code>. Do not increment <code>correction_attempts</code>.</li><li>Preallocate <code>assessment_id</code>, create reviewer session id <code>reviewer:&lt;goalId&gt;:&lt;assessment_id&gt;</code>, transition the goal&#39;s planner session to <code>Dormant</code>, set <code>active_card_run.phase = &#39;reviewer&#39;</code>, and invoke the reviewer. On <code>needs_corrections</code> (step 6) the planner is transitioned back to <code>Running</code> before being resumed.</li><li>Persist the assessment on the goal card as <code>result.review</code>.</li><li>On <code>pass</code>, reset <code>correction_attempts</code>, mark the goal <code>done</code>, mirror <code>status_text</code> from the accepted <code>report_goal_done</code> onto the card, persist <code>latest_self_report</code>, unwind to the parent with <code>done</code>.</li><li>On <code>needs_corrections</code>, increment <code>correction_attempts</code>. If <code>correction_attempts &lt;= max_review_retries</code>, transition the planner back to <code>Running</code> with the assessment available in Goal Context as <code>latest_review_result</code> and <code>resume_reason: &#39;reviewer_correction&#39;</code>, set <code>active_card_run.phase = &#39;planner&#39;</code>, and loop back to step 4 of §7.3. The rejected <code>report_goal_done</code>&#39;s <code>status_text</code> and <code>latest_self_report</code> are NOT mirrored onto the card.</li><li>If <code>correction_attempts &gt; max_review_retries</code>, write <code>pending_subtree_correction</code> notes on the origin goal and every strict ancestor goal, mark the origin <code>changed</code>, mark the planner <code>Dormant</code>, and unwind to the parent with <code>failed</code> and <code>failure_kind: &#39;review_retries_exhausted&#39;</code>. The reviewer issues stay on <code>card.result.review</code>; they are NOT copied into the tool_result.</li></ol><p><strong>Reviewer interrupt recovery.</strong> If a service restart (or supervisor force-cancel) interrupts a reviewer session before <code>result.review</code> is persisted, the runtime resumes the goal&#39;s planner with <code>resume_reason: &#39;reviewer_interrupted&#39;</code> and a <code>reviewer_interrupted</code> synthetic note in Goal Context. The planner inspects the subtree, confirms the work is still complete, and re-issues <code>report_goal_done</code>; the runtime re-runs the gates and reviewer with a fresh <code>assessment_id</code>. The interrupted reviewer session is discarded.</p><p><code>max_review_retries</code> defaults to 3 (runtime config). Per-card override via <code>card.metadata.max_review_retries</code>. Semantics: a value of <code>N</code> means the runtime allows up to <code>N</code> planner resumes after a <code>needs_corrections</code> verdict; the <code>N + 1</code>-th consecutive verdict fails the activation. <code>correction_attempts</code> is reset to 0 on <code>pass</code> and on <code>restart_card</code>.</p><p>Persisted JSON uses <code>snake_case</code>; the in-memory TypeScript mirror uses <code>camelCase</code>.</p><h2 id="_8-goal-context-schema" tabindex="-1">8. Goal Context Schema <a class="header-anchor" href="#_8-goal-context-schema" aria-label="Permalink to &quot;8. Goal Context Schema&quot;">​</a></h2><p>Goal Context is rebuilt for every planner creation or runtime resume. Keep it intentionally basic for now:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;goal&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;project&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  parent_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  depth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  description</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  acceptance</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tags</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  priority</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  depends_on</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  blocks</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  child_card_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  notes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextNote</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_self_report</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_review_result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewAssessment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  correction_attempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  max_review_retries</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  resume_reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;initial&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer_correction&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;analyst_directive&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_changed&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;service_restart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  child_card_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextNote</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pending_subtree_correction&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      origin_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      issues</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnalystIssue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_changed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      descendant_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_not_ready&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reasons</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SubtreeReadinessReason</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer_interrupted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">assessment_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;analyst_note&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;done&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerIssue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  severity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;info&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;warning&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocker&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  recommendation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pass&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;needs_corrections&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  achieved</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  issues</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerIssue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  assessment_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>latest_self_report</code> is the most recent terminal report this planner filed for this goal, persisted on the card. It replaces the old <code>PlanningResult</code>/<code>latest_planning_result</code> field, which is deleted.</p><p>Resume mechanics:</p><ul><li>Every runtime resume appends one synthetic user turn to the planner&#39;s message log containing the freshly rebuilt Goal Context block plus a one-line resume reason.</li><li>Prior synthetic context turns are kept. Compaction handles drift.</li><li>Resume reasons: <code>initial</code>, <code>reviewer_correction</code>, <code>analyst_directive</code>, <code>subtree_changed</code>, <code>reviewer_interrupted</code>, <code>service_restart</code>.</li><li>The <code>subtree_changed</code> resume_reason is used when an ancestor planner is reactivated to react to descendants that landed in <code>changed</code> status under the analyst pause-mutate-unpause pattern (§10).</li></ul><p>Only the directive note kinds shown above are injected into <code>notes</code>; ordinary notes remain available through note tools.</p><p>Durable evidence is one of, scoped to descendants of the goal:</p><ul><li>A registered artifact.</li><li>A registered attachment.</li><li>For terminal cards, a non-null <code>result.executor</code> object that validates against the executor result schema for that card type.</li><li>For goal cards, a <code>result.review.result === &#39;pass&#39;</code> assessment.</li></ul><h2 id="_9-planner-prompt" tabindex="-1">9. Planner Prompt <a class="header-anchor" href="#_9-planner-prompt" aria-label="Permalink to &quot;9. Planner Prompt&quot;">​</a></h2><p>The planner prompt must teach the new boundary:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>You are the planner for goal &quot;&lt;title&gt;&quot; (id: &lt;id&gt;).</span></span>
<span class="line"><span>You own the cards in this goal subtree.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Create child cards for work. When a child card should be worked,</span></span>
<span class="line"><span>call activate_card(card_id). The runtime will pass control to the</span></span>
<span class="line"><span>agent that owns that card (executor or another planner) and return</span></span>
<span class="line"><span>one terminal result: done, failed, or blocked. The same planner may</span></span>
<span class="line"><span>resume on a later activate_card on the same goal; executor sessions</span></span>
<span class="line"><span>are one-shot per activation.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>For non-trivial implementation, test, doc, data, research, ops, or</span></span>
<span class="line"><span>architecture work, create a terminal card and call activate_card on</span></span>
<span class="line"><span>it. For larger work, create a goal card and call activate_card on it.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>When you finish this goal, call report_goal_done with a summary,</span></span>
<span class="line"><span>status_text (a short progress line the operator and your parent</span></span>
<span class="line"><span>will see on the card), and evidence_card_ids. The runtime then</span></span>
<span class="line"><span>checks: (1) no descendant is in \`blocked\` or \`changed\` status;</span></span>
<span class="line"><span>(2) every evidence card is durable.</span></span>
<span class="line"><span>If either check fails, you receive a tool error (subtree_not_ready</span></span>
<span class="line"><span>or invalid_evidence) and you must fix the state without consuming a</span></span>
<span class="line"><span>review retry. If both checks pass, the runtime runs the reviewer.</span></span>
<span class="line"><span>If reviewer corrections are needed, the runtime may resume you</span></span>
<span class="line"><span>inside the same activation with reviewer feedback in Goal Context.</span></span>
<span class="line"><span>You do not call the reviewer yourself.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If activate_card returns failed for a child, read the child card&#39;s</span></span>
<span class="line"><span>result.review (if any) to see the reviewer&#39;s issues, then decide</span></span>
<span class="line"><span>whether to add notes and re-activate the child, restructure with</span></span>
<span class="line"><span>delete_card/restart_card, or report this goal as failed or blocked.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If you are resumed with \`resume_reason: &#39;reviewer_interrupted&#39;\`,</span></span>
<span class="line"><span>a previous reviewer was interrupted before persisting its result.</span></span>
<span class="line"><span>Inspect the subtree and the card; if the work is still complete,</span></span>
<span class="line"><span>call report_goal_done again. The runtime will re-run the gates</span></span>
<span class="line"><span>and reviewer.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If a descendant lands in \`changed\` status (you will see this in the</span></span>
<span class="line"><span>child_card_tree and as a subtree_changed note), decide whether to</span></span>
<span class="line"><span>re-activate the affected descendant, restructure, or escalate.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If this goal cannot be completed, call report_goal_failed or</span></span>
<span class="line"><span>report_goal_blocked, again with a status_text field.</span></span></code></pre></div><p>The project planner is an ordinary goal planner whose goal is the project card. Its prompt gets one additional instruction: it schedules top-level goals by creating top-level cards and calling <code>activate_card</code> on each one. It is brought to <code>Running</code> by the runtime when a <code>lets_dance</code> directive is pending; the runtime does not auto-spawn it on boot.</p><h2 id="_10-analyst-lets-dance-and-operator-paths" tabindex="-1">10. Analyst, <code>lets_dance</code>, and Operator Paths <a class="header-anchor" href="#_10-analyst-lets-dance-and-operator-paths" aria-label="Permalink to &quot;10. Analyst, \`lets_dance\`, and Operator Paths&quot;">​</a></h2><p>Analyst tools:</p><ul><li><code>lets_dance()</code>: records a <code>lets_dance</code> directive on the project card. The runtime&#39;s idle safe-tick consumes the directive and calls <code>activate_card(projectCardId)</code> itself once the runtime is idle and unpaused. The system is kicked off this way; there is no other project-bootstrap path.</li><li><code>mark_goal_needs_corrections(goalId, issues, note?)</code>: writes <code>pending_subtree_correction</code> notes on the origin goal card and each strict ancestor goal card and may flip the origin card from <code>done</code>, <code>running</code>, or <code>blocked</code> to <code>changed</code>. Resumes no planner.</li><li><code>mark_project_needs_corrections(issues, note?)</code>: writes a directive note on the project card. The runtime decides on its next safe tick whether to call <code>activate_card(projectCardId)</code> (same conditions as <code>lets_dance</code>).</li><li><code>pause_runtime()</code> / <code>resume_runtime()</code>: toggle the global pause gate.</li></ul><p>Analyst interaction with mutable state uses the pause-mutate-unpause pattern:</p><ol><li><code>pause_runtime</code>.</li><li>Write notes, flip statuses (e.g. mark descendants <code>changed</code>), record directives, edit card fields.</li><li><code>resume_runtime</code>.</li></ol><p>On resume, if a planner is <code>Running</code> or <code>AwaitingChild</code>, the runtime appends a synthetic user turn to the active planner&#39;s message log summarizing the analyst&#39;s mutations (note kinds: <code>analyst_note</code>, <code>pending_subtree_correction</code>, and/or <code>subtree_changed</code> with the affected <code>descendant_card_ids</code>).</p><p><strong>Note injection.</strong> Synthetic notes are queued on the target planner&#39;s session and injected as a synthetic user turn the next time that planner is about to receive an assistant turn (a freshly landed <code>tool_result</code>, a fresh activation, or a runtime resume). Injection is one-shot per note: once the synthetic turn is appended, the note is removed from the queue and is not redelivered. The card-side status (e.g. <code>changed</code>) and persisted correction records remain on the card; the queued note is just the delivery vehicle into the LLM conversation.</p><p><strong>Note routing.</strong> A synthetic note is queued on the session of the deepest planner whose goal subtree contains the affected card. If that planner is currently <code>Dormant</code>, the note remains queued and is delivered the next time the planner is brought to <code>Running</code> by <code>activate_card</code>.</p><p><strong><code>changed</code> consumption.</strong> When <code>activate_card(card_id)</code> is called on a card in status <code>changed</code>, the runtime transitions the card to <code>running</code> and consumes the <code>changed</code> marker; any queued <code>subtree_changed</code> notes referencing this card are delivered once and then discarded.</p><p>Guaranteeing semantic consistency of these mutations against in-flight runtime state is deferred to a future stage (§14).</p><p>The <code>changed</code> flow is recursive: when a descendant is marked <code>changed</code>, ancestor goal cards get a <code>subtree_changed</code> note. When control unwinds to an ancestor planner via <code>activate_card</code>&#39;s outcome, that ancestor sees the note in its next Goal Context and decides what to do (re-activate the affected descendant, restructure, or escalate).</p><p>Notifications never resume a planner and never block <code>report_goal_done</code>. The notification acknowledgement gate from earlier drafts is removed.</p><p>Analyst issues and HTTP issues use one canonical shape:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnalystIssue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  severity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;info&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;warning&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocker&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>The analyst tool and the HTTP endpoint accept the same array shape; <code>flagged_by</code> is derived server-side from the authenticated session and is never accepted from the client body.</p><h2 id="_11-http-api" tabindex="-1">11. HTTP API <a class="header-anchor" href="#_11-http-api" aria-label="Permalink to &quot;11. HTTP API&quot;">​</a></h2><ul><li><p>Remove the legacy runtime dispatch endpoint (<code>/api/runtime/dispatch</code>).</p></li><li><p>Add <code>POST /api/runtime/lets_dance</code>. No body required. Returns <code>{directive_recorded: true, runtime_status: &#39;idle&#39; | &#39;running&#39; | &#39;paused&#39;}</code>. The runtime calls <code>activate_card(projectCardId)</code> itself on its next safe tick.</p></li><li><p>Add <code>POST /api/runtime/goals/:id/needs_corrections</code> with body <code>{issues: AnalystIssue[], note?: string}</code>. Records notes and returns <code>{origin_goal_id, notes_recorded_on_goal_ids, status_transition}</code>. The response never includes <code>resumed_planner_session_id</code>.</p></li><li><p>Add <code>POST /api/runtime/project/needs_corrections</code> with body <code>{issues: AnalystIssue[], note?: string}</code>. Records a project directive note and returns <code>{directive_recorded: true, runtime_status: &#39;idle&#39; | &#39;running&#39; | &#39;paused&#39;}</code>. The runtime decides on its next safe tick whether to activate the project card.</p></li><li><p>Add <code>POST /api/runtime/pause</code> and <code>POST /api/runtime/resume</code>. Returns the updated <code>RuntimeState</code>.</p></li><li><p>Add <code>GET /api/runtime/card-runs</code>, returning a typed union:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardRunsResponse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_card_run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_breadcrumb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardBreadcrumbNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// server-computed, project -&gt; leaf</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  dormant_planners</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DormantPlannerRow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  cards_with_pending_corrections</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PendingCorrectionRow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardBreadcrumbNode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DormantPlannerRow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  goal_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  planner_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_self_report</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PendingCorrectionRow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  note_count</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  last_note_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div></li></ul><h2 id="_12-tests" tabindex="-1">12. Tests <a class="header-anchor" href="#_12-tests" aria-label="Permalink to &quot;12. Tests&quot;">​</a></h2><h3 id="unit-tests" tabindex="-1">Unit Tests <a class="header-anchor" href="#unit-tests" aria-label="Permalink to &quot;Unit Tests&quot;">​</a></h3><ul><li><code>activate_card</code> authorization and subtree guard.</li><li>Terminal-card activation maps executor outcomes to terminal parent outcomes, mirrors executor <code>status_text</code> onto the card, and sets then clears <code>active_card_run</code> exactly once.</li><li>Goal-card activation loops planner -&gt; reviewer -&gt; planner internally on reviewer corrections.</li><li>Retry exhaustion returns <code>failed</code> with <code>failure_kind: &#39;review_retries_exhausted&#39;</code>; the tool_result does NOT duplicate reviewer issues; the issues are present on <code>card.result.review</code>; the origin card status is <code>changed</code>.</li><li><code>report_goal_done</code> with a descendant in <code>blocked</code> or <code>changed</code> status returns a <code>tool_error</code> of kind <code>subtree_not_ready</code> with the offending card id and status.</li><li><code>report_goal_done</code> with bad evidence returns a <code>tool_error</code> of kind <code>invalid_evidence</code>, leaves <code>active_card_run</code> set, and does not increment <code>correction_attempts</code>.</li><li>Subtree-readiness gate runs before evidence; both gates run before the reviewer.</li><li>A rejected <code>report_goal_done</code> does NOT mirror <code>status_text</code> and does NOT update <code>latest_self_report</code>; only an accepted <code>report_goal_*</code> (or accepted executor terminal result) updates those fields.</li><li><code>activate_card</code> on a completed terminal card without a preceding <code>restart_card</code> returns a <code>tool_error</code> of kind <code>terminal_card_requires_restart</code>.</li><li><code>activate_card</code> on a card in status <code>running</code> returns a <code>tool_error</code> of kind <code>card_already_active</code>.</li><li><code>activate_card</code> on a card in status <code>changed</code> consumes the <code>changed</code> marker, transitions the card to <code>running</code>, and the queued <code>subtree_changed</code> note(s) referencing that card are delivered to the planner exactly once and then removed from the queue.</li><li>Restart while <code>active_card_run.phase === &#39;reviewer&#39;</code> with no persisted <code>result.review</code> resumes the planner with <code>resume_reason: &#39;reviewer_interrupted&#39;</code> and a <code>reviewer_interrupted</code> note; the planner re-issues <code>report_goal_done</code>, gates re-run, a fresh <code>assessment_id</code> is used, the discarded reviewer session is not persisted.</li><li><code>cancel_card</code> refuses any target whose subtree contains the active leaf; otherwise it accepts <code>backlog</code>, <code>active</code>, or <code>changed</code> and writes a synthetic <code>latest_self_report</code> with reason <code>cancelled</code> when none exists.</li><li><code>report_goal_*</code> mirrors <code>status_text</code> onto the goal card and records <code>latest_self_report</code> with summary, status_text, and timestamp.</li><li>Goal Context surfaces each child card&#39;s <code>status_text</code>, <code>latest_self_report</code>, and includes <code>resume_reason</code>.</li><li><code>AgentSession</code> rejects <code>parent_session_id</code>, <code>parent_tool_call_id</code>, and legacy statuses (<code>active</code>, <code>done</code>, <code>failed</code>, <code>awaiting_review</code>, <code>suspended</code>).</li><li><code>RuntimeState</code> validates the single-<code>active_card_run</code> shape and rejects legacy current fields and any persisted ancestor chain.</li><li>Goal Context includes the basic fields, latest review/self report, filtered correction notes, <code>subtree_changed</code> notes, and retry counters.</li><li>Durable evidence validation rejects siblings and cards without registered artifacts, attachments, valid executor results, or a passed review for goal cards.</li><li><code>delete_card</code> / <code>restart_card</code> honour the leaf and subtree preconditions and behave per §3.1. <code>restart_card</code> accepts the <code>changed</code> status as input.</li><li>No <code>set_status_text</code> tool is exposed; planner attempts to call it produce a hard tool error.</li><li>No <code>acknowledge_notification</code> tool is exposed; planner attempts to call it produce a hard tool error.</li></ul><h3 id="integration-tests" tabindex="-1">Integration Tests <a class="header-anchor" href="#integration-tests" aria-label="Permalink to &quot;Integration Tests&quot;">​</a></h3><ul><li>Boot sequence: runtime starts idle; no planner is <code>Running</code>; no card is activated until the analyst calls <code>lets_dance</code>. After <code>lets_dance</code>, the runtime itself calls <code>activate_card(projectCardId)</code> on its next safe tick.</li><li>Project planner creates a top-level goal and calls <code>activate_card</code> on it; <code>active_card_run</code> points at the project card immediately before the call and at the top-level goal immediately after.</li><li>Nested goal activation preserves the single-<code>Running</code>-planner invariant across arbitrary depth, with <code>active_card_run</code> always pointing at the deepest active card; unwinding restores <code>active_card_run</code> to each ancestor in turn.</li><li>Reviewer corrections are handled internally until pass; the parent sees only <code>done</code>.</li><li>Reviewer corrections exhaust the retry limit; the parent sees <code>failed</code>, ancestors receive correction notes, the origin card is <code>changed</code>, and the child card has <code>result.review</code> set.</li><li>Parent re-activates a <code>Dormant</code> child after adding a note; the same planner resumes with the new Goal Context.</li><li>Analyst pause-mutate-unpause: while a planner is <code>Running</code>, the analyst pauses, marks a descendant card <code>changed</code>, and unpauses. The planner&#39;s next turn delivers a synthetic note with the affected descendant ids; the planner can call <code>activate_card</code> on the descendant.</li><li>Analyst late re-open records notes without activating any card; a later <code>mark_project_needs_corrections</code> records a directive that the runtime consumes on its next idle safe tick (if the project has already gone <code>Dormant</code>).</li><li>Restart with an orphan <code>activate_card</code> call repairs exactly one parent <code>tool_result</code> from persisted card/session state.</li><li>Restart after <code>report_goal_done</code> but before reviewer invocation re-runs the acceptance gates and invokes the reviewer without making the planner <code>Running</code>.</li><li>Restart after reviewer invocation but before <code>result.review</code> is persisted re-runs the reviewer-interrupt recovery path: the planner is resumed with <code>resume_reason: &#39;reviewer_interrupted&#39;</code>, the planner re-calls <code>report_goal_done</code>, the gates and reviewer re-run with a fresh <code>assessment_id</code>, and the parent eventually sees <code>done</code>.</li><li>Pause/resume preserves the in-flight leaf and process records; shell processes that started before pause continue running; their terminal <code>tool_result</code> is buffered until resume.</li><li>Stuck planner force-cancel emits exactly one synthetic <code>failed</code> tool_result with <code>failure_kind: &#39;planner_force_cancelled&#39;</code> and clears <code>active_card_run</code>; the parent becomes <code>Running</code> on its next turn.</li><li>Status text propagation: a deep terminal card&#39;s executor <code>status_text</code> is mirrored onto its card; the project planner&#39;s next Goal Context shows the value via <code>child_card_tree</code>.</li></ul><h3 id="delete-old-tests" tabindex="-1">Delete Old Tests <a class="header-anchor" href="#delete-old-tests" aria-label="Permalink to &quot;Delete Old Tests&quot;">​</a></h3><ul><li>Dispatch-loop tests.</li><li><code>PlannerResult</code> / <code>PlanningResult</code> parser tests.</li><li><code>start_planner</code>, <code>start_executor</code>, <code>run_card</code>, <code>set_status_text</code>, <code>acknowledge_notification</code>, <code>resumePlannerSession</code>, Tier-A/Tier-B, <code>awaiting_review</code>, <code>suspended</code>, <code>running-blocked</code>, and <code>active_planner_sessions</code> tests.</li><li>Tests expecting backward compatibility with old <code>.saivage/</code> state.</li><li>Tests asserting on the notification acknowledgement gate.</li></ul><h2 id="_13-runtime-events" tabindex="-1">13. Runtime Events <a class="header-anchor" href="#_13-runtime-events" aria-label="Permalink to &quot;13. Runtime Events&quot;">​</a></h2><p>Add or keep these event kinds:</p><ul><li><code>card_activation_started</code></li><li><code>card_activation_phase_changed</code></li><li><code>card_activation_completed</code></li><li><code>project_run_completed</code> — payload <code>{ project_card_id, result: &#39;done&#39; | &#39;failed&#39; | &#39;blocked&#39;, summary: string, failure_kind?: CardActivationOutcome[&#39;failure_kind&#39;], blocked_reason?: string }</code>.</li><li><code>card_cancelled</code></li><li><code>reviewer_interrupted</code></li><li><code>goal_report_rejected</code> (with <code>kind: &#39;subtree_not_ready&#39; | &#39;invalid_evidence&#39;</code>)</li><li><code>goal_review_needs_corrections</code></li><li><code>correction_notes_recorded</code></li><li><code>review_retry_exhausted</code></li><li><code>planner_direct_write</code></li><li><code>card_destructive_delete</code></li><li><code>card_restarted</code></li><li><code>card_marked_changed</code></li><li><code>planner_session_force_cancelled</code></li><li><code>planner_compaction_throttled</code></li><li><code>goal_reopened_by_analyst</code></li><li><code>planner_stale</code></li><li><code>status_text_updated</code></li><li><code>lets_dance_received</code></li><li><code>runtime_idle_tick_consumed_directive</code></li><li><code>process_started</code></li><li><code>process_timed_out</code></li><li><code>process_killed</code></li><li><code>process_reconciled_dead</code> — emitted only during restart-time durable process reconciliation when a previously running process is found dead/lost; persisted alongside the synthetic terminal record.</li><li><code>process_reattach_rejected</code> — emitted only during restart-time durable process reconciliation when identity matches but reattach is rejected; persisted alongside the synthetic terminal record.</li></ul><p>Durable async process terminal records and restart-time process reconciliation audit events are current scope.</p><p>Remove Tier-A/Tier-B-era correction events, legacy dispatch-loop events, and any <code>notification_acknowledged</code> events tied to the removed gate.</p><p>Event payloads share a common envelope <code>{ event_id: string; runtime_session_id: string; at: string }</code> plus event-specific fields named after the event kind. Examples: <code>project_run_completed { result, summary }</code>; <code>card_cancelled { card_id, cancelled_by_session_id }</code>; <code>goal_report_rejected { kind, goal_card_id, session_id }</code>; <code>card_destructive_delete { card_id, archive_path }</code>.</p><h2 id="_14-phasing" tabindex="-1">14. Phasing <a class="header-anchor" href="#_14-phasing" aria-label="Permalink to &quot;14. Phasing&quot;">​</a></h2><ol><li>Clean-state boot and schema deletion: remove legacy state readers, add <code>.saivage</code> discard behavior, rewrite validators.</li><li>Card model and status_text: extend <code>Card</code> with the three status_text fields; remove <code>set_status_text</code>; wire runtime mirroring from executor terminal response and <code>report_goal_*</code>.</li><li>Planner tools and prompt: add <code>activate_card</code>, drop <code>start_*</code>, any earlier <code>run_card</code> name, <code>set_status_text</code>, and <code>acknowledge_notification</code>; update planner and project-planner prompts to include <code>status_text</code> in every terminal report.</li><li>Runtime activation implementation: terminal cards, goal cards, single <code>active_card_run</code>, single-active-planner enforcement, unwinding, <code>delete_card</code> and <code>restart_card</code>.</li><li>Reviewer loop and acceptance-gate tool errors: subtree-readiness gate, evidence gate, preallocated assessment ids, retry counters, retry exhaustion with reviewer issues left on the card, status flip to <code>changed</code>.</li><li>Analyst and API paths: correction notes, <code>lets_dance</code> bootstrap endpoint, pause-mutate-unpause synthetic-note injection, card-run operator endpoint with the typed union response, <code>AnalystIssue</code> shape unification.</li><li>Restart and orphan repair: orphan <code>activate_card</code> repair (including reviewer-interrupt recovery per §7.4) plus durable process reconciliation and identity probes.</li><li>Test deletion and rebuild: remove obsolete tests, add focused unit and integration coverage, then run build and docs verification.</li></ol><p>Future stages (not in this redesign):</p><ul><li>Split <code>.saivage/</code> into per-concern subdirectories (sessions/cards/runtime/archive/notifications).</li><li>Reset-planner feature to truncate a <code>Dormant</code> planner&#39;s LLM message log so a re-activation starts fresh.</li><li>Continuous improvement agent: the <code>continuousImprovement</code> config flag is reserved for a future stage that auto-emits <code>lets_dance</code> on a schedule. The flag is read but has no runtime effect today.</li><li>Project-level recovery loop after <code>failed</code> / <code>blocked</code> outcomes, and the operator-notification / analyst-handoff side of <code>project_run_completed</code>.</li><li>Analyst-mutation consistency guarantees against in-flight runtime state.</li><li>Subtree-readiness <code>pending_subprocess</code> gate extension for the goal card&#39;s own processes. Durable <code>ProcessRecord</code>s now exist and are observable, but <code>report_goal_done</code> gate ordering remains <code>subtree_not_ready</code> → <code>invalid_evidence</code> → reviewer until this future gate extension lands.</li><li>Cancel-cascade through running ancestors. Cancelling a card whose subtree contains the active leaf would require cascading cancellation through the running ancestor chain and the active leaf itself; deferred.</li><li>Artifact and attachment registration schemas (their on-disk layout and the registration API). Evidence validation (§8) references registered artifacts and attachments without specifying the registration schema here.</li></ul><h2 id="_15-ninth-round-review-fixes" tabindex="-1">15. Ninth-Round Review Fixes <a class="header-anchor" href="#_15-ninth-round-review-fixes" aria-label="Permalink to &quot;15. Ninth-Round Review Fixes&quot;">​</a></h2><p>The ninth review against the eighth-round design surfaced nine findings (five major plus four medium/low) plus a request to explicitly track everything deferred to future stages. Each is addressed by the sections noted, or explicitly deferred to §14.</p><table tabindex="0"><thead><tr><th>#</th><th>Issue</th><th>Resolution</th></tr></thead><tbody><tr><td>1</td><td>Durable async process layer (<code>wait_for_process</code>, <code>kill_process</code>, persistent <code>ProcessRecord</code>, restart identity probes, process reattach, pause-time buffering of process results) is implemented separately from activation gates.</td><td>Current scope includes durable process records and terminal buffering; the descendant-process subtree gate remains deferred to §14.</td></tr><tr><td>2</td><td><code>pending_tool_result</code> payload on <code>ActiveCardRun</code> was needed only by the deferred async-process pause-buffering case.</td><td>Removed. Pause is a pure global gate; in-flight tool dispatch completes synchronously then the runtime stops scheduling new LLM turns (§4).</td></tr><tr><td>3</td><td><code>ReviewerIssue</code> was referenced but never typed.</td><td><code>ReviewerIssue</code> schema added: <code>{ summary, severity: &#39;info&#39; | &#39;warning&#39; | &#39;blocker&#39;, evidence_card_id?, recommendation? }</code>. <code>ReviewerResult.issues: ReviewerIssue[]</code> (§8; agents.md §10).</td></tr><tr><td>4</td><td><code>status_text</code> and <code>latest_self_report</code> write timing on rejected <code>report_goal_done</code> was ambiguous.</td><td>Explicit: only an accepted <code>report_goal_*</code> (or accepted executor terminal result) mirrors <code>status_text</code> and writes <code>latest_self_report</code>. Rejected reports (<code>subtree_not_ready</code>, <code>invalid_evidence</code>, <code>needs_corrections</code>) write neither (§7.4, §12; agents.md §10).</td></tr><tr><td>5</td><td>Cancelling an ancestor of the active leaf was implied but its cascade through running ancestors was unspecified.</td><td>Cancel-cascade through running ancestors deferred (§14). <code>cancel_card</code> now refuses any target whose subtree contains the active leaf and only accepts <code>backlog</code>/<code>active</code>/<code>changed</code> (§3.1, §12; agents.md §7.1).</td></tr><tr><td>6</td><td>Subtree-readiness gate recursed into descendants for live subprocesses.</td><td>Removed the recursion. Subgoals cannot return <code>done</code> while holding live processes (their own readiness gate would reject), so the ancestor gate only needs to check the goal card&#39;s own state. With async processes deferred, the gate reduces to &quot;no descendant in <code>blocked</code> or <code>changed</code> status&quot; (§3.3, §7.4; agents.md §8.2).</td></tr><tr><td>7</td><td>Synthetic-note injection timing distinguished resume vs. next LLM turn.</td><td>Simplified: notes are injected the next time the target planner&#39;s LLM conversation has room to admit a synthetic user turn (a freshly landed <code>tool_result</code>, a fresh activation, or a runtime resume). Each note is one-shot (§10; agents.md §11).</td></tr><tr><td>8</td><td>Reviewer interrupt (restart while <code>phase: &#39;reviewer&#39;</code>) had no documented recovery path.</td><td>Recovery path documented: planner is resumed with <code>resume_reason: &#39;reviewer_interrupted&#39;</code> and a <code>reviewer_interrupted</code> synthetic note. The planner inspects the subtree, re-issues <code>report_goal_done</code>, and the gates and reviewer re-run with a fresh <code>assessment_id</code> (§7.1, §7.4, §8, §9; agents.md §10, §11, §12).</td></tr><tr><td>9</td><td>Several minor: terminal-card-without-restart returned an unnamed tool error; <code>activate_card</code> on <code>running</code> card had unnamed error; <code>restart_card</code> did not say what happens to <code>latest_self_report</code>; <code>project_run_completed</code> payload was thin; cross-ref bug &quot;§10, §14 of the plan&quot;.</td><td>Named errors <code>terminal_card_requires_restart</code> and <code>card_already_active</code> (§5; agents.md §4.2). <code>restart_card</code> on goal cards clears <code>latest_self_report</code> (§3.1; agents.md §7.1). <code>project_run_completed</code> payload typed (§13; agents.md §6). Cross-ref fixed (agents.md §8.2).</td></tr></tbody></table><h2 id="_16-eighth-round-review-fixes" tabindex="-1">16. Eighth-Round Review Fixes <a class="header-anchor" href="#_16-eighth-round-review-fixes" aria-label="Permalink to &quot;16. Eighth-Round Review Fixes&quot;">​</a></h2><p>The eighth review against the seventh-round design surfaced sixteen findings (three major plus thirteen medium/low). Each is addressed by the sections noted.</p><table tabindex="0"><thead><tr><th>#</th><th>Issue</th><th>Resolution</th></tr></thead><tbody><tr><td>1</td><td>Caller-edge contract underspecified for nested unwinding.</td><td>Caller-edge reconstruction algorithm documented: <code>CardStore.getParent</code> → <code>planner:&lt;parentCardId&gt;</code> (<code>AwaitingChild</code>) → unique unresolved <code>activate_card</code> tool call. No persisted stack. Each card has at most one activation in flight at a time (§4; agents.md §6).</td></tr><tr><td>2</td><td>Root activation / system-caller path missing.</td><td>System-caller path documented: on project-card termination the runtime persists the outcome on the project card, clears <code>active_card_run</code>, emits <code>project_run_completed</code>, and returns to idle. Operator-notification flow deferred (§4, §13, §14; agents.md §6, §17).</td></tr><tr><td>3</td><td><code>activate_card</code> source-status matrix undefined.</td><td>Added status-transition table in §5 and agents.md §4.2 covering <code>backlog</code>/<code>active</code>/<code>changed</code>/terminal/<code>running</code>.</td></tr><tr><td>4</td><td>Status transitions on activation not specified.</td><td>Same table as #3: cards transition to <code>running</code> on activation; terminal outcome replaces <code>running</code> on unwind.</td></tr><tr><td>5</td><td>Pause buffering not persisted in <code>ActiveCardRun</code>.</td><td>Added <code>pending_tool_result?: { tool_call_id, payload, received_at }</code> to <code>ActiveCardRun</code>; durable across restart (§4; agents.md §6).</td></tr><tr><td>6</td><td>Synthetic-note routing unspecified.</td><td>Note routing rule: queued on the deepest planner whose goal subtree contains the affected card; if <code>Dormant</code>, queued until next <code>activate_card</code> (§10; agents.md §11).</td></tr><tr><td>7</td><td><code>changed</code> / note resolution lifecycle unclear.</td><td><code>changed</code> is consumed on <code>activate_card</code> (card transitions to <code>running</code>). Notes are injected once into the LLM conversation and then discarded (§5, §10; agents.md §4.2, §11).</td></tr><tr><td>8</td><td>Rejected <code>report_goal_*</code> could mirror stale <code>status_text</code>.</td><td>Explicit rule: rejected <code>report_goal_*</code> does NOT mirror <code>status_text</code> or update <code>latest_self_report</code> (§3.2; agents.md §8.1).</td></tr><tr><td>9</td><td><code>delete_card</code> required cancellation but no cancel op existed.</td><td>Added <code>cancel_card(card_id)</code> planner tool; required prerequisite for deleting non-terminal cards (§3.1; agents.md §7, §7.1).</td></tr><tr><td>10</td><td>Readiness gate missed processes on the goal card itself.</td><td>Gate now checks descendants AND the goal card itself for live subprocesses (§3.3; agents.md §8.2).</td></tr><tr><td>11</td><td>Project correction vs deferred recovery loop reconciliation.</td><td><code>mark_project_needs_corrections</code> records a directive consumed by the same idle safe-tick as <code>lets_dance</code>; full operator/analyst recovery loop deferred (§10, §14; agents.md §11).</td></tr><tr><td>12</td><td>Reviewer-phase planner lifecycle not explicit.</td><td>Planner transitions to <code>Dormant</code> at reviewer start, back to <code>Running</code> on <code>needs_corrections</code> retry (§7.4).</td></tr><tr><td>13</td><td>Missing schemas: <code>ReviewAssessment</code>, <code>DormantPlannerRow</code>, <code>PendingCorrectionRow</code>, event payloads.</td><td>All four added (§8, §11, §13; agents.md §9, §14).</td></tr><tr><td>14</td><td><code>pending_subtree_correction</code> note shape too thin.</td><td>Expanded to <code>{ kind, origin_card_id, issues: AnalystIssue[], body, at }</code> (§8; agents.md §9).</td></tr><tr><td>15</td><td><code>status_text</code> fields non-nullable for new cards.</td><td>All three <code>status_text</code> fields made nullable; <code>null</code> until first accepted terminal report (§5; agents.md §4.1).</td></tr><tr><td>16</td><td>Intro wording conflated planner and executor recurrence.</td><td>Reworded: planners recur on the same goal; executors are one-shot per activation (§1; agents.md intro).</td></tr></tbody></table><h2 id="_17-seventh-round-review-fixes" tabindex="-1">17. Seventh-Round Review Fixes <a class="header-anchor" href="#_17-seventh-round-review-fixes" aria-label="Permalink to &quot;17. Seventh-Round Review Fixes&quot;">​</a></h2><p>The seventh review (against the leaf-only <code>activate_card</code> design) surfaced 13 issues plus two additional design directives. Each is addressed by the sections noted, or explicitly deferred.</p><table tabindex="0"><thead><tr><th>#</th><th>Issue</th><th>Resolution</th></tr></thead><tbody><tr><td>1</td><td>Root/project activation contract underspecified.</td><td>Runtime is idle on boot. Analyst calls <code>lets_dance</code>; the runtime itself calls <code>activate_card(projectCardId)</code> on its next safe tick (§7.1, §10, §11; agents.md §11).</td></tr><tr><td>2</td><td>Single <code>active_card_run</code> loses nested caller edges.</td><td>Unwinding is explicit: on leaf termination the runtime walks one step up via <code>CardStore.getParent</code>, reconstructs the parent&#39;s <code>ActiveCardRun</code>, and delivers the synthesized outcome (§4, §7.2, §7.3; agents.md §3, §6).</td></tr><tr><td>3</td><td>Unwinding from leaf to parent underspecified.</td><td>Same as #2; recursive unwinding documented in §4 and agents.md §3.</td></tr><tr><td>4</td><td>Analyst/root activation conflicts with synchronous parent-tool semantics.</td><td>Analyst always works through pause-mutate-unpause; the runtime, not the analyst, calls <code>activate_card(projectCardId)</code>. Consistency of mutations is deferred (§10, §14; agents.md §11).</td></tr><tr><td>5</td><td>Project correction endpoint response was stale/narrow.</td><td>Endpoint records a directive only; runtime decides; response is <code>{directive_recorded, runtime_status}</code>. Operator/analyst fix loop after <code>failed</code>/<code>blocked</code> is deferred (§11, §14).</td></tr><tr><td>6</td><td>Pause buffering not persisted/typed.</td><td>Pause is a global gate; in-flight processes keep running and their terminal <code>tool_result</code> is buffered until resume (§4; agents.md §6, §12).</td></tr><tr><td>7</td><td>Notification acknowledgement tool was referenced but not exposed.</td><td>Removed entirely. No <code>acknowledge_notification</code> tool; no <code>unacknowledged_notification</code> gate; notifications never block <code>report_goal_done</code> (§2 Remove, §3.2, §10).</td></tr><tr><td>8</td><td>Evidence tool error could loop forever.</td><td>Accepted: governed by the standard stuck-supervisor path; no separate cap (§3.2; agents.md §8.1).</td></tr><tr><td>9</td><td><code>PlanningResult</code> remained orphaned in Goal Context.</td><td>Removed. Replaced by <code>latest_self_report: LatestSelfReport</code> populated from the most recent terminal <code>report_goal_*</code> call (§8; agents.md §9).</td></tr><tr><td>10</td><td><code>continuousImprovement</code> config flag is orphaned.</td><td>Kept in config as a deferred no-op; documented as Future Stages (§14; agents.md §17).</td></tr><tr><td>11</td><td><code>running-blocked</code> reactivation path ambiguous.</td><td>Renamed to <code>changed</code>. The card is NOT immediately re-activated; ancestor planners see a <code>subtree_changed</code> note in their next Goal Context and decide whether to call <code>activate_card</code>. Reactivation is recursive and eventual (§5, §8, §10; agents.md §4.2, §11).</td></tr><tr><td>12</td><td>Analyst correction against the active card not defined.</td><td>Pause-mutate-unpause appends a synthetic note to the active planner&#39;s message log on resume; the planner sees it on its next LLM turn and replans (§10; agents.md §11).</td></tr><tr><td>13</td><td>Stale wording: file map &quot;active card chain&quot;, &quot;project wake path&quot;, changelog entries.</td><td>Replaced throughout. File map mentions <code>lets_dance</code> and single active card-run; analyst tools row says <code>lets_dance</code> bootstrap (agents.md §16).</td></tr><tr><td>A</td><td>Runtime owns project activation timing.</td><td>Runtime safe-tick loop activates the project card itself when idle and a <code>lets_dance</code> / project-correction directive is pending (§7.1, §10; agents.md §11).</td></tr><tr><td>B</td><td>Runtime acceptance gates beyond the reviewer.</td><td>New <code>subtree_not_ready</code> tool error rejects <code>report_goal_done</code> if any descendant has a live subprocess or is in <code>blocked</code>/<code>changed</code> status. Runs before evidence and the reviewer; does not consume <code>max_review_retries</code> (§3.3, §7.4; agents.md §8.2).</td></tr><tr><td>C</td><td>Status text writes were a special tool action.</td><td>Removed <code>set_status_text</code>. <code>status_text</code> is a required field on every executor terminal response and every <code>report_goal_*</code> call; the runtime mirrors it onto the card (§3, §5; agents.md §4.1, §4.3, §7).</td></tr></tbody></table><h2 id="_18-sixth-round-review-fixes-recap" tabindex="-1">18. Sixth-Round Review Fixes (Recap) <a class="header-anchor" href="#_18-sixth-round-review-fixes-recap" aria-label="Permalink to &quot;18. Sixth-Round Review Fixes (Recap)&quot;">​</a></h2><p>The sixth review surfaced 18 issues against an earlier draft. They were addressed by the previous revision of this plan and remain in force unless superseded by §17 (Seventh-Round), §16 (Eighth-Round), or §15 (Ninth-Round) above. Highlights:</p><ol><li>Restart receipts for <code>run_card</code>: reset-planner feature deferred to §14.</li><li>Nested active runs reduced to a single leaf <code>active_card_run</code>; ancestor <code>AwaitingChild</code> derived from each planner&#39;s unresolved <code>activate_card</code> tool call.</li><li>No child-status aggregation: <code>card.status_text</code> is canonical; child-tree mirror surfaces it in Goal Context.</li><li>Retry exhaustion does not duplicate reviewer issues into the <code>failed</code> tool_result.</li><li>Evidence and (originally) notification gates promoted to <code>report_goal_done</code> tool errors that do not consume retries; the notification arm has since been removed entirely (§17.7).</li><li><code>run_card</code> renamed to <code>activate_card</code>.</li><li><code>.saivage/</code> subdirectory layout deferred.</li><li>Destructive card ops fully specified in §3.1.</li><li>Project planner modelled like any other planner; bootstrap updated to runtime-owned <code>lets_dance</code> flow (§17.1).</li><li>Retry semantics: <code>N</code> resumes allowed; <code>N+1</code>-th <code>needs_corrections</code> fails the activation.</li><li>Goal Context resume mechanics: one synthetic user turn per resume.</li><li>Durable evidence restricted to descendants.</li><li>Re-entrant <code>activate_card</code> documented as the normal correction path.</li><li>Pause vs. force-cancel semantics clarified.</li><li>Planner tool authority policy aligned with executor&#39;s.</li><li><code>AnalystIssue</code> unified across analyst tool and HTTP endpoint.</li><li><code>GET /api/runtime/card-runs</code> returns a typed union with a server-computed breadcrumb.</li><li>Changelog wording softened.</li></ol><h2 id="_19-fifth-round-review-fixes-recap" tabindex="-1">19. Fifth-Round Review Fixes (Recap) <a class="header-anchor" href="#_19-fifth-round-review-fixes-recap" aria-label="Permalink to &quot;19. Fifth-Round Review Fixes (Recap)&quot;">​</a></h2><p>The fifth review found 21 design issues. They are addressed by the clean-slate activation model and refined further by the sixth-, seventh-, eighth-, and ninth-round fixes (§18, §17, §16, §15). Highlights:</p><ol><li>Parent planners no longer call planners; they activate cards through <code>activate_card</code>.</li><li><code>start_planner</code> / <code>start_executor</code> removed.</li><li>Reviewer/planner interaction is inside the runtime-owned activation.</li><li><code>needs_corrections</code> is no longer a parent-visible child outcome.</li><li>Single-active-planner invariant preserved without an explicit chain structure.</li><li>Planner parentage derived from the card tree.</li><li><code>parent_session_id</code> / <code>parent_tool_call_id</code> removed from <code>AgentSession</code>.</li><li>Runtime caller edges live on <code>active_card_run</code> and <code>ProcessRecord</code>.</li><li><code>active_planner_sessions</code> replaced by a single <code>active_card_run</code> plus derived ancestor walking.</li><li><code>AwaitingReview</code> removed.</li><li>Reviewer session ids use preallocated assessment ids.</li><li><code>max_review_retries</code> defaults to 3, per-card overridable.</li><li>Retry exhaustion returns <code>failed</code> to the parent; reviewer issues stay on the card.</li><li>Goal Context has a basic, explicit schema with <code>resume_reason</code>.</li><li>Durable evidence definition fixed.</li><li>Clean-slate boot discards old <code>.saivage/</code> state.</li><li>HTTP responses no longer expose <code>resumed_planner_session_id</code>.</li><li>Continuous improvement reserved as a deferred future stage (§17.10).</li><li>Tests rewritten around activations and internal reviewer retries.</li><li>Stale Tier-A/Tier-B and parent-only-resumption language removed.</li><li>Runtime and architecture docs aligned.</li></ol><h2 id="_20-validation" tabindex="-1">20. Validation <a class="header-anchor" href="#_20-validation" aria-label="Permalink to &quot;20. Validation&quot;">​</a></h2><p>Run from <code>/home/salva/g/ml/saivage-v3</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:verify</span></span></code></pre></div><p>When code changes implement the plan, also run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NODE_OPTIONS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">--experimental-vm-modules</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --runInBand</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --forceExit</span></span></code></pre></div>`,150)])])}const u=s(n,[["render",d]]);export{k as __pageData,u as default};

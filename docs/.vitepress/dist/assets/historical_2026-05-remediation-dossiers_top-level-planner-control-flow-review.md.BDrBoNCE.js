import{_ as a,o as t,c as r,ae as o}from"./chunks/framework.B2ul6ABY.js";const u=JSON.parse('{"title":"Top-Level Planner Control Flow Review","description":"","frontmatter":{},"headers":[],"relativePath":"historical/2026-05-remediation-dossiers/top-level-planner-control-flow-review.md","filePath":"historical/2026-05-remediation-dossiers/top-level-planner-control-flow-review.md"}'),n={name:"historical/2026-05-remediation-dossiers/top-level-planner-control-flow-review.md"};function i(l,e,s,c,d,h){return t(),r("div",null,[...e[0]||(e[0]=[o('<blockquote><p><strong>Historical/Audit Artifact — Not Current Operator Instructions</strong><br> This page records a pre-repair control-flow review. It is not authoritative for current Saivage v3 behavior unless a current active doc explicitly revalidates it against current source and tests.</p></blockquote><h1 id="top-level-planner-control-flow-review" tabindex="-1">Top-Level Planner Control Flow Review <a class="header-anchor" href="#top-level-planner-control-flow-review" aria-label="Permalink to &quot;Top-Level Planner Control Flow Review&quot;">​</a></h1><p>Status note: this is a working recovery note, not an authoritative specification. Existing specs, plans, and docs may be stale; source code, live runtime behavior, and the user&#39;s latest instructions take precedence until the documentation is audited and updated.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The current Saivage v3 runtime is not yet organized around a durable strategic planner that stays in control of a project until the project is actually complete. It can create child cards, execute ready terminal cards, and then re-enter the same goal loop, but the architecture is still queue-centric rather than planner-centric.</p><p>The older Saivage v2 design had the stronger idea: the planner calls subgoal and task execution through explicit MCP-style dispatch tools. That model makes the planner an active controller that can inspect, dispatch, wait, and replan. Saivage v3 currently approximates that behavior by having the planner emit cards and the runtime execute whichever terminal cards are ready. This loses an important control boundary.</p><h2 id="what-happened-in-the-live-run" tabindex="-1">What Happened In The Live Run <a class="header-anchor" href="#what-happened-in-the-live-run" aria-label="Permalink to &quot;What Happened In The Live Run&quot;">​</a></h2><p>The the target project live run did show partial progress:</p><ul><li>The planner created <code>architecture-1</code> and <code>research-1</code>.</li><li>Executors wrote real files and marked those cards done.</li><li>The goal planner later created <code>code-1</code> and <code>code-2</code>.</li><li><code>code-1</code> completed and <code>code-2</code> started.</li></ul><p>That means the runtime can perform at least one goal-local replan. The deeper failure is that this behavior is accidental and fragile:</p><ul><li>Parent control returns only through the <code>runGoal</code> loop after <code>executeReadyCards</code> returns.</li><li>There is no explicit project-level planner dispatch cycle attached to the <code>project</code> card.</li><li>There is no first-class MCP control tool for <code>dispatch_goal</code>, <code>dispatch_card</code>, <code>wait_for_dispatch</code>, or <code>inspect_artifacts</code>.</li><li>Executor final-result parsing can fail even after tool writes succeed, trapping the runtime in retries and provider fallback attempts.</li><li>The UI shows statuses but not the generated workspace files as directly inspectable evidence.</li></ul><h2 id="current-runtime-shape" tabindex="-1">Current Runtime Shape <a class="header-anchor" href="#current-runtime-shape" aria-label="Permalink to &quot;Current Runtime Shape&quot;">​</a></h2><p>In <code>src/utils/runtime.ts</code>, <code>dispatchGoal</code> starts <code>runGoal(goalId)</code>. <code>runGoal</code> then:</p><ol><li>Activates the goal.</li><li>Invokes the planner for that goal.</li><li>Applies <code>created_cards</code> and <code>updated_cards</code>.</li><li>Executes ready terminal cards through <code>executeReadyCards</code>.</li><li>Loops back to the same goal planner while <code>status</code> remains <code>continue</code>.</li><li>Invokes reviewer only when the planner says <code>done</code>.</li></ol><p>This is useful but insufficient. It treats planning as a JSON-producing step inside a scheduler loop. The planner cannot explicitly call a subgoal/task execution tool, observe the tool result, and then continue reasoning in the same control conversation.</p><h2 id="main-design-gap" tabindex="-1">Main Design Gap <a class="header-anchor" href="#main-design-gap" aria-label="Permalink to &quot;Main Design Gap&quot;">​</a></h2><p>The project card is not a live strategic control point. Project-level planning exists mostly as backlog auto-dispatch and continuous-improvement behavior after top-level goals complete. A long-running project with one active goal does not have a project planner that can inspect the whole tree, decide the only goal is incomplete, and create further goals or tasks through control tools.</p><p>The application needs a control-plane layer where planning and dispatch are explicit runtime operations. Cards should remain the durable data model, but the planner should operate through validated tools rather than only returning a static JSON diff.</p><h2 id="secondary-gaps" tabindex="-1">Secondary Gaps <a class="header-anchor" href="#secondary-gaps" aria-label="Permalink to &quot;Secondary Gaps&quot;">​</a></h2><h3 id="structured-result-brittleness" tabindex="-1">Structured Result Brittleness <a class="header-anchor" href="#structured-result-brittleness" aria-label="Permalink to &quot;Structured Result Brittleness&quot;">​</a></h3><p>Executors can call tools, write files, and verify output, then return final text that fails <code>ExecutorResult</code> validation. The runtime currently treats that as invocation failure and tries fallbacks. This is wasteful and can prevent the parent planner from regaining control.</p><h3 id="evidence-not-first-class-enough" tabindex="-1">Evidence Not First-Class Enough <a class="header-anchor" href="#evidence-not-first-class-enough" aria-label="Permalink to &quot;Evidence Not First-Class Enough&quot;">​</a></h3><p>Generated files are not surfaced as first-class card evidence in the UI. Operators should be able to inspect files directly from the card that generated them, along with verification commands and tool output summaries.</p><h3 id="fallback-configuration-leakage" tabindex="-1">Fallback Configuration Leakage <a class="header-anchor" href="#fallback-configuration-leakage" aria-label="Permalink to &quot;Fallback Configuration Leakage&quot;">​</a></h3><p>The live run repeatedly tried providers that were known unavailable. For focused recovery work, the configured provider set must be authoritative: Codex-only should mean no Copilot/opencode fallback attempts.</p><h2 id="recommended-direction" tabindex="-1">Recommended Direction <a class="header-anchor" href="#recommended-direction" aria-label="Permalink to &quot;Recommended Direction&quot;">​</a></h2><p>Use explicit planner-dispatch control as the design target for v3:</p><ul><li>Add an in-process planning/control MCP service in v3.</li><li>Give planners explicit tools for state inspection, card/subgoal creation, dispatch, wait, review, and artifact inspection.</li><li>Let parent planners suspend while child work executes and resume with structured results.</li><li>Keep cards as durable state, but stop relying on implicit queue exhaustion as the main planning boundary.</li><li>Add UI file evidence inspection so card completion is auditable by humans.</li></ul>',28)])])}const m=a(n,[["render",i]]);export{u as __pageData,m as default};

import{_ as e,o as i,c as a,ae as t}from"./chunks/framework.B2ul6ABY.js";const k=JSON.parse('{"title":"Agent Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"agents.md","filePath":"agents.md"}'),n={name:"agents.md"};function r(l,s,d,o,h,c){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="agent-architecture" tabindex="-1">Agent Architecture <a class="header-anchor" href="#agent-architecture" aria-label="Permalink to &quot;Agent Architecture&quot;">​</a></h1><p>This document describes the target Saivage v3 agent architecture. The companion implementation plan is <a href="./planner-redesign-plan.html">Planner Redesign Implementation Plan</a>.</p><p>The design is card-centered. A planner owns a goal subtree, but it does not directly invoke another planner or executor. It asks the runtime to <code>activate_card(card_id)</code>. The runtime passes control to the agent that owns the card, handles any planner/reviewer loop required for it, and eventually returns one terminal result to the caller.</p><p><code>activate_card</code> is intentionally not called <code>run_card</code>. Planner sessions are long-lived: the same planner session that owns a goal card may receive control again on a subsequent <code>activate_card</code> call from its parent. Executor sessions are one-shot per activation; a re-activation of a terminal card always opens a new executor session. The verb names the control-transfer, not a single execution.</p><h2 id="_1-roles" tabindex="-1">1. Roles <a class="header-anchor" href="#_1-roles" aria-label="Permalink to &quot;1. Roles&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Role</th><th>Responsibility</th><th>Lifetime</th><th>Started by</th></tr></thead><tbody><tr><td>Planner</td><td>Owns one goal subtree: creates and edits child cards, activates child cards, and reports the goal result.</td><td>Long-lived for the goal. Goes <code>Dormant</code> after <code>report_goal_done</code>, <code>report_goal_failed</code>, or <code>report_goal_blocked</code>. Resumed by a later <code>activate_card</code> on the same goal.</td><td>A parent planner calling <code>activate_card(goalCardId)</code>. The project card has no parent planner; the runtime activates it when an analyst <code>lets_dance</code> directive is pending (§11).</td></tr><tr><td>Executor</td><td>Performs one terminal card.</td><td>One-shot per card activation. A re-activation of the same terminal card opens a new executor session.</td><td>Runtime, inside <code>activate_card</code> for a terminal card.</td></tr><tr><td>Reviewer</td><td>Assesses a planner&#39;s completed goal.</td><td>One-shot per assessment.</td><td>Runtime, inside <code>activate_card</code> after <code>report_goal_done</code> clears the runtime acceptance gates (§8.2).</td></tr><tr><td>Analyst</td><td>Operator-facing assistant for creating cards, notes, correction directives, and the project kickoff.</td><td>Session-oriented.</td><td>Operator.</td></tr><tr><td>Project planner</td><td>The planner for the project root. It is an ordinary goal planner whose goal is the project card.</td><td>Long-lived for the project.</td><td>The runtime, when it is idle and an analyst <code>lets_dance</code> (or persisted project-correction) directive is pending; the runtime calls <code>activate_card(projectCardId)</code> itself (§11).</td></tr></tbody></table><h2 id="_2-component-view" tabindex="-1">2. Component View <a class="header-anchor" href="#_2-component-view" aria-label="Permalink to &quot;2. Component View&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  UI[Web UI / REST API] --&gt; RT[Runtime]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  RT --&gt; CS[CardStore]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  RT --&gt; NC[NotificationCenter]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  RT --&gt; PR[ProcessRunner]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  RT --&gt; AA[AgentAdapter]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AA --&gt; PL[Planner sessions]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AA --&gt; EX[Executor sessions]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AA --&gt; RV[Reviewer sessions]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  PL --&gt;|activate_card card_id| RT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  PL --&gt;|card, workspace tools| RT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  EX --&gt;|artifacts, attachments, result+status_text| RT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  RV --&gt;|review assessment| RT</span></span></code></pre></div><p>The Runtime is the only dispatcher. The AgentAdapter runs LLM sessions and hands runtime tools back to Runtime for authoritative execution.</p><h2 id="_3-primary-sequence" tabindex="-1">3. Primary Sequence <a class="header-anchor" href="#_3-primary-sequence" aria-label="Permalink to &quot;3. Primary Sequence&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Parent as Parent planner</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Runtime as Runtime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Child as Child planner</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Exec as Executor</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Review as Reviewer</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Parent-&gt;&gt;Runtime: activate_card(cardId)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  alt terminal card</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Runtime-&gt;&gt;Exec: run executor session</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Exec--&gt;&gt;Runtime: executor result (incl. status_text)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Runtime--&gt;&gt;Parent: tool_result done/failed/blocked</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  else goal card</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Runtime-&gt;&gt;Child: ensure or resume planner session</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    loop until terminal or retry limit</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      Child--&gt;&gt;Runtime: report_goal_done | _failed | _blocked</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      alt failed or blocked</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Runtime--&gt;&gt;Parent: tool_result failed/blocked</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      else done</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Note over Runtime: §8.2 acceptance gates (subtree readiness, evidence)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        alt any gate fails</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          Runtime--&gt;&gt;Child: tool_error (planner stays Running)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        else gates pass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          Runtime-&gt;&gt;Review: reviewer session</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          Review--&gt;&gt;Runtime: pass or needs_corrections</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          alt pass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            Runtime--&gt;&gt;Parent: tool_result done</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          else needs_corrections and retries remain</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            Runtime-&gt;&gt;Child: resume with review in Goal Context</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          else needs_corrections and retries exhausted</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            Runtime--&gt;&gt;Parent: tool_result failed</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span></code></pre></div><p>From the parent planner&#39;s point of view, <code>activate_card</code> is synchronous: one tool call receives one tool result. The runtime can persist and resume the physical work across service restarts, but the caller sees a single terminal outcome per call.</p><p>When the active leaf terminates, the runtime sets <code>active_card_run</code> to point at the parent card-run reconstructed from the parent&#39;s pending <code>activate_card</code> tool call (or to <code>null</code> if the leaf is the project card), and delivers the synthesized <code>CardActivationOutcome</code> to that parent. The parent planner transitions back to <code>Running</code> on its next turn. Unwinding is recursive: a chain of finished children produces a chain of <code>tool_result</code> deliveries, one per ancestor.</p><h2 id="_4-card-model" tabindex="-1">4. Card Model <a class="header-anchor" href="#_4-card-model" aria-label="Permalink to &quot;4. Card Model&quot;">​</a></h2><p>Cards form the durable hierarchy. Parentage is derived from the card tree with <code>CardStore.getParent(card_id)</code>. Session records never carry a durable parent pointer.</p><p>Goal cards can contain child cards and are worked by planner sessions. Terminal cards are worked by executor sessions.</p><h3 id="_4-1-card-fields" tabindex="-1">4.1 Card Fields <a class="header-anchor" href="#_4-1-card-fields" aria-label="Permalink to &quot;4.1 Card Fields&quot;">​</a></h3><p>In addition to the usual title, description, tags, priority, acceptance, dependencies, and <code>result</code> fields, every card carries. Card priority is a whole number on a 0-100 scale, where larger numbers sort and surface as higher priority work:</p><ul><li><code>status_text: string | null</code> — short free-form progress line written by the runtime from the most recent accepted terminal agent report on that card. <code>null</code> until the first accepted terminal report. It is the canonical surface for upward visibility (see §4.3).</li><li><code>status_text_updated_at: string | null</code> — ISO timestamp of the last write, or <code>null</code> while <code>status_text</code> is <code>null</code>.</li><li><code>status_text_author_session_id: string | null</code> — id of the session whose terminal report produced the current <code>status_text</code>, or <code>null</code> while <code>status_text</code> is <code>null</code>.</li></ul><p><code>status_text</code> is set by the runtime, never written directly by agents. Agents include a <code>status_text</code> field in their terminal response (executor result, <code>report_goal_done / _failed / _blocked</code>); the runtime mirrors it onto the card. There is no <code>set_status_text</code> planner tool.</p><h3 id="_4-2-statuses" tabindex="-1">4.2 Statuses <a class="header-anchor" href="#_4-2-statuses" aria-label="Permalink to &quot;4.2 Statuses&quot;">​</a></h3><ul><li><code>backlog</code>: not currently active.</li><li><code>active</code>: ready for a planner to choose.</li><li><code>running</code>: work is in progress.</li><li><code>changed</code>: the card&#39;s state was externally modified (by the analyst or by a descendant subtree correction) since its planner last saw it. The card is not immediately re-activated; instead the parent planner (and, recursively, any ancestor) sees a <code>subtree_changed</code> note in its next Goal Context and decides whether to call <code>activate_card</code> on the affected descendant.</li><li><code>done</code>: accepted complete.</li><li><code>failed</code>: ended in failure.</li><li><code>blocked</code>: cannot proceed without external change.</li><li><code>cancelled</code>: operator cancelled.</li></ul><p><code>changed</code> can land on any card: an origin goal flagged by the analyst, or any descendant whose state was edited under pause. Ancestors of a <code>changed</code> card receive <code>subtree_changed</code> notes but keep their current status.</p><p>Status transitions driven by <code>activate_card</code>:</p><table tabindex="0"><thead><tr><th>Source status</th><th>Effect of <code>activate_card(card_id)</code></th></tr></thead><tbody><tr><td><code>backlog</code>, <code>active</code></td><td>Card transitions to <code>running</code>. Activation proceeds.</td></tr><tr><td><code>changed</code></td><td>Card transitions to <code>running</code>; the <code>changed</code> marker is consumed by the activation and any queued <code>subtree_changed</code> notes referencing this card are delivered once and discarded (§11).</td></tr><tr><td><code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code></td><td>Re-activation is allowed for goal cards as the normal correction path: the <code>Dormant</code> planner is resumed with fresh notes and the card transitions back to <code>running</code>. For terminal cards, <code>restart_card</code> is required first to clear <code>result.executor</code>; otherwise <code>activate_card</code> returns a <code>tool_error</code> of kind <code>terminal_card_requires_restart</code>.</td></tr><tr><td><code>running</code></td><td>Tool error of kind <code>card_already_active</code>: only one activation may be in flight per card.</td></tr></tbody></table><p>The terminal activation outcome (<code>done</code>, <code>failed</code>, or <code>blocked</code>) replaces <code>running</code> when control returns to the parent.</p><h3 id="_4-3-status-reporting-contract" tabindex="-1">4.3 Status Reporting Contract <a class="header-anchor" href="#_4-3-status-reporting-contract" aria-label="Permalink to &quot;4.3 Status Reporting Contract&quot;">​</a></h3><ul><li>Every executor&#39;s terminal response must include a <code>status_text</code> field. The runtime writes it onto the card.</li><li>Every <code>report_goal_done / _failed / _blocked</code> call must include a <code>status_text</code> field. The runtime writes it onto the goal card.</li><li>The runtime does not require status updates between terminal reports. Mid-run progress is not surfaced; the operator and ancestor planners see the most recent terminal <code>status_text</code> only.</li><li><code>status_text</code> is the field the operator console and ancestor planners read to learn how a descendant card last reported. The runtime does not aggregate or synthesize a separate &quot;child summary&quot; field; the authoritative surface is the card itself.</li></ul><h2 id="_5-planner-lifecycle" tabindex="-1">5. Planner Lifecycle <a class="header-anchor" href="#_5-planner-lifecycle" aria-label="Permalink to &quot;5. Planner Lifecycle&quot;">​</a></h2><p>Planner sessions are long-lived and deterministic:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>planner:&lt;goal_card_id&gt;</span></span></code></pre></div><p>Lifecycle states:</p><ul><li><code>Running</code>: the session may receive LLM turns. At most one planner is in this state globally.</li><li><code>AwaitingChild</code>: the session owns an outstanding <code>activate_card</code> or process wait and receives no LLM turns.</li><li><code>Dormant</code>: the session reported done, failed, or blocked. It can be resumed by a later <code>activate_card</code> on the same goal.</li><li><code>ForceCancelled</code>: the supervisor or operator terminated the session.</li></ul><p>Runtime pause is a global gate (<code>RuntimeState.paused === true</code>) that stops the runtime from delivering new LLM turns to any session. It is not a per-session state; on resume the runtime simply continues from whichever session was about to receive its next turn.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">stateDiagram-v2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  [*] --&gt; Running: activate_card on this goal</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Running --&gt; AwaitingChild: activate_card child or process wait</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AwaitingChild --&gt; Running: tool_result delivered</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Running --&gt; Dormant: report_goal_done / failed / blocked</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Dormant --&gt; Running: activate_card same goal</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Running --&gt; ForceCancelled: supervisor/operator</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AwaitingChild --&gt; ForceCancelled: supervisor/operator</span></span></code></pre></div><p>There is no reviewer-wait planner state. While a reviewer is running, the goal&#39;s active card-run record has <code>phase: &#39;reviewer&#39;</code>; the planner session itself is <code>Dormant</code>.</p><h2 id="_6-runtime-state" tabindex="-1">6. Runtime State <a class="header-anchor" href="#_6-runtime-state" aria-label="Permalink to &quot;6. Runtime State&quot;">​</a></h2><p>The runtime starts in an idle state: <code>active_card_run === null</code>, no planner <code>Running</code>. It stays idle until an analyst <code>lets_dance</code> (or a persisted project-correction directive) triggers the runtime to call <code>activate_card(projectCardId)</code> itself (§11).</p><p>At any moment thereafter, at most one card-run is doing real work. Every ancestor planner up to the project root is <code>AwaitingChild</code> of its direct child. The runtime stores only the leaf card-run; the ancestor chain is derived on demand from the card hierarchy.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RuntimeState</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  paused</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  notice</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_card_run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  runtime_status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;running&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;force_cancelled&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  phase</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;executor&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  caller_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  caller_tool_call_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  planner_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  executor_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  reviewer_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  correction_attempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  started_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  last_turn_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>active_card_run</code> is <code>null</code> when the runtime is idle (e.g. before the project card has been activated, or after the project planner goes <code>Dormant</code>). Otherwise it holds the single card that is currently executing, planning, or under review.</p><p>Pause is a pure global gate. In-flight LLM turns finish their current tool dispatch and then the runtime stops scheduling new turns. Durable process terminal results that arrive while paused are buffered in the process registry and delivered exactly once to the planner route when the runtime resumes. Acceptance-gate extensions remain future stages (§17).</p><p><strong>Caller-edge reconstruction.</strong> When the active leaf terminates, the runtime resolves which planner receives the synthesized <code>tool_result</code> purely from the card hierarchy and persisted session logs:</p><ol><li><code>parentCardId = CardStore.getParent(active_card_run.card_id)</code>.</li><li>If <code>parentCardId === null</code> the leaf is the project card; see the system-caller path below.</li><li><code>parentSessionId = &quot;planner:&quot; + parentCardId</code>. That session must be <code>AwaitingChild</code>; its message log contains exactly one unresolved <code>activate_card(active_card_run.card_id)</code> tool call.</li><li>That call&#39;s <code>session_id</code> and <code>tool_call_id</code> become the caller edge.</li><li>The runtime reconstructs <code>ActiveCardRun</code> for <code>parentCardId</code> with <code>phase: &#39;planner&#39;</code>, sets <code>active_card_run</code> to that record, and delivers the synthesized <code>CardActivationOutcome</code> as the parent&#39;s next <code>tool_result</code>. Unwinding repeats recursively when the parent in turn reports <code>done</code>, <code>failed</code>, or <code>blocked</code>.</li></ol><p>This works because each card has at most one activation in flight at any moment: a card may be activated multiple times sequentially, but never concurrently. There is therefore no ambiguity in which <code>activate_card</code> tool call to resolve. Restart repair uses the same algorithm; no persisted caller stack is required.</p><p><strong>System-caller path.</strong> When the terminating leaf is the project card (<code>parentCardId === null</code>), there is no parent planner to receive a tool_result. The runtime persists the outcome on the project card (status, <code>result.review</code> when applicable, <code>latest_self_report</code>), clears <code>active_card_run</code> to <code>null</code>, emits <code>project_run_completed { project_card_id, result, summary, failure_kind?, blocked_reason?, latest_self_report?, at }</code>, and returns to idle. The specific operator-notification or analyst-handoff flow after a project completion is a future stage (§17).</p><p><strong>Execution invariant.</strong> A planner session may have at most one unresolved <code>activate_card</code> tool call at any time. Shell execution is represented by durable process records: <code>start_and_wait</code> and <code>run_project_command</code> remain available for synchronous command waits, and long-lived process control is exposed through <code>wait_for_process</code> and <code>kill_process</code>. Acceptance-gate extensions remain deferred to §17.</p><p>Ancestors are derived:</p><ul><li>The breadcrumb from project root to leaf is <code>walkParents(leaf.card_id)</code> using <code>CardStore.getParent</code>.</li><li>An ancestor planner&#39;s <code>AwaitingChild</code> status is implied by the fact that it has an outstanding <code>activate_card</code> tool_call with no matching <code>tool_result</code> in its message log. Restart repair uses the same evidence.</li></ul><p><code>caller_session_id</code> and <code>caller_tool_call_id</code> are the transient edges that let restart repair deliver exactly one <code>tool_result</code> to the parent planner of the leaf. They live on <code>active_card_run</code> only and are not part of <code>AgentSession</code>.</p><p><code>ForceCancelled</code> applies to the leaf. Runtime pause is global (<code>RuntimeState.paused</code>); it does not change <code>active_card_run</code> and does not change any session&#39;s lifecycle state. While paused, no new LLM turns are delivered; any tool dispatch that was already in flight on the runtime side completes synchronously, after which the runtime stops scheduling new turns. Shell processes that outlive an LLM turn continue running; terminal process results are buffered during pause and delivered once on resume.</p><p><strong>Planner no-progress recovery.</strong> If a planner LLM turn cannot make progress because it repeats the same tool-call fingerprint or exhausts the bounded tool-call loop, the adapter records a <code>model_issue</code>, adds a final-answer prompt that forbids further tool calls, and requires the next assistant payload to be the normal planner result envelope. The runtime parses only that coerced envelope (<code>status</code>, <code>summary</code>, <code>created_cards</code>, <code>updated_cards</code>, and optional <code>blocked_reason</code>); raw <code>toolCalls</code> objects are never a runtime state transition. If the only planner tool call is a deferred <code>activate_card</code>, the adapter instead synthesizes the same <code>status: &#39;continue&#39;</code> envelope so the active card run remains coherent while the child activation proceeds. Force-cancel emits exactly one synthetic <code>failed</code> tool_result to the leaf&#39;s parent and clears <code>active_card_run</code>; the parent then becomes <code>Running</code> again on its next turn. If the cancelled leaf is the project card, the runtime persists a synthetic <code>failed</code> outcome on the project card and emits <code>project_run_completed</code> (no tool_result is delivered).</p><h2 id="_7-planner-tools" tabindex="-1">7. Planner Tools <a class="header-anchor" href="#_7-planner-tools" aria-label="Permalink to &quot;7. Planner Tools&quot;">​</a></h2><p>Planner tools are subtree-scoped to the planner&#39;s goal.</p><p>Card mutation, inspection, and notes:</p><ul><li><code>create_card(parent_id, type, title, description, status?, depends_on?, priority?, tags?, acceptance?)</code></li><li><code>edit_card(card_id, patch)</code></li><li><code>cancel_card(card_id)</code> — see §7.1.</li><li><code>delete_card(card_id)</code> — see §7.1.</li><li><code>restart_card(card_id)</code> — see §7.1.</li><li><code>add_note(card_id, body)</code></li><li><code>list_cards(filter?)</code></li><li><code>get_card(card_id)</code></li><li><code>get_tree(card_id?)</code></li><li><code>list_card_history(card_id)</code></li><li><code>get_card_history_entry(entry_id)</code></li><li><code>diff_card(card_id, from, to)</code></li></ul><p>Workspace and process tools:</p><ul><li><code>list_project_files</code></li><li><code>read_project_file</code></li><li><code>write_project_file</code></li><li><code>start_and_wait</code></li><li><code>run_project_command</code></li><li><code>wait_for_process</code></li><li><code>kill_process</code></li></ul><p>Durable async process handling is current scope. <code>ProcessRecord</code>s are persisted under <code>.saivage/runtime/</code>, include salted <code>command_hash</code>, <code>cwd_canonical</code>, <code>started_at_monotonic</code>, owner and terminal metadata, and are reconciled on restart with identity probes. Reattach mismatch or failure produces a single synthetic lost <code>process_failed</code> terminal.</p><p>Runtime dispatch:</p><ul><li><code>activate_card(card_id) -&gt; CardActivationOutcome</code></li></ul><p>Completion tools (each carries <code>status_text</code> for the runtime to mirror onto the goal card):</p><ul><li><code>report_goal_done({summary, status_text, evidence_card_ids})</code></li><li><code>report_goal_failed({summary, status_text, error?})</code></li><li><code>report_goal_blocked({summary, status_text, blocked_reason})</code></li></ul><p>There is no <code>set_status_text</code> tool. Status flows only at terminal report (§4.3).</p><p>The planner has no reviewer invocation tool. Review is automatic inside the active card activation, after the runtime acceptance gates pass.</p><h3 id="_7-1-destructive-card-operations" tabindex="-1">7.1 Destructive Card Operations <a class="header-anchor" href="#_7-1-destructive-card-operations" aria-label="Permalink to &quot;7.1 Destructive Card Operations&quot;">​</a></h3><p>These tools are subtree-scoped: the target card must lie inside the calling planner&#39;s goal subtree.</p><ul><li><p><code>cancel_card(card_id)</code></p><ul><li>Allowed transitions: from <code>backlog</code>, <code>active</code>, or <code>changed</code> to <code>cancelled</code>.</li><li>Refused for the active leaf, for any card whose subtree contains the active leaf, for cards in status <code>running</code>, and for cards already in a terminal status (<code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code>). Cancellation cascading onto running ancestors of the active leaf is a future stage (§17).</li><li>Effect: marks the card <code>cancelled</code>, persists a synthetic <code>latest_self_report</code> of result <code>failed</code> (reason <code>cancelled</code>) when none exists, leaves the planner session (if any) <code>Dormant</code>, and emits <code>card_cancelled</code>. This is the prerequisite to <code>delete_card</code> for non-terminal cards.</li></ul></li><li><p><code>delete_card(card_id)</code></p><ul><li>Refused if <code>card_id</code> or any descendant equals <code>RuntimeState.active_card_run?.card_id</code>, or any descendant planner session is not <code>Dormant</code>.</li><li>Allowed targets: cards in <code>backlog</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, or <code>cancelled</code>. To delete an <code>active</code> or <code>changed</code> card, the caller must first call <code>cancel_card</code>. Deleting a <code>running</code> card is not allowed in this stage (it would imply ancestor-cascade cancellation; see §17).</li><li>Effect: archives the full card record (fields, notes, result, evidence references) under <code>.saivage/archive/cards/&lt;card_id&gt;.json</code>, removes the card from <code>CardStore</code>, and emits <code>card_destructive_delete</code>.</li><li>Cascades: descendants are archived and removed under the same rules; if any descendant fails its precondition, the whole operation is rejected with no partial mutation.</li></ul></li><li><p><code>restart_card(card_id)</code></p><ul><li>Allowed transitions: from <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>cancelled</code>, or <code>changed</code> back to <code>active</code>.</li><li>Refused while the card is the active leaf. Because every card has at most one activation in flight at any moment and <code>running</code> is held only by the active leaf and its in-flight ancestors, no other <code>running</code> source is reachable in this stage.</li><li>Effect on terminal cards: clears <code>result.executor</code>, leaves <code>status_text</code> as-is (the next executor run will overwrite it), leaves history intact.</li><li>Effect on goal cards: clears <code>result.review</code>, clears <code>latest_self_report</code>, resets <code>correction_attempts</code> to 0, and leaves the planner session in its current <code>Dormant</code> state. The planner is not woken; the parent must call <code>activate_card(card_id)</code> to give control back.</li><li>Reset of a planner&#39;s internal LLM message log is <strong>not</strong> part of <code>restart_card</code>. It is listed as a future stage in §17.</li></ul></li></ul><h2 id="_8-card-activation-outcomes" tabindex="-1">8. Card Activation Outcomes <a class="header-anchor" href="#_8-card-activation-outcomes" aria-label="Permalink to &quot;8. Card Activation Outcomes&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardActivationOutcome</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;done&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">evidence_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      failure_kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;executor_failed&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner_failed&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;review_retries_exhausted&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;planner_force_cancelled&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;service_restart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blocked_reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span></code></pre></div><p><code>needs_corrections</code> is a reviewer verdict, not a parent-visible activation outcome. The runtime handles it by resuming the same goal planner inside the current activation until the retry limit is reached. Retry exhaustion is reported as <code>failed</code> with <code>failure_kind: &#39;review_retries_exhausted&#39;</code>.</p><p>Reviewer issues, when retries are exhausted, are not duplicated into the <code>failed</code> payload. The parent reads them from the failed child&#39;s <code>card.result.review</code>.</p><h3 id="_8-1-evidence-validation-is-a-tool-error" tabindex="-1">8.1 Evidence Validation Is a Tool Error <a class="header-anchor" href="#_8-1-evidence-validation-is-a-tool-error" aria-label="Permalink to &quot;8.1 Evidence Validation Is a Tool Error&quot;">​</a></h3><p><code>report_goal_done</code> validates the planner&#39;s evidence references before any reviewer is invoked. If validation fails, the runtime returns a <code>tool_error</code> on the <code>report_goal_done</code> call with kind <code>invalid_evidence</code> and a per-card breakdown of which references were rejected and why. The planner stays <code>Running</code>; <code>active_card_run</code> is not cleared; the reviewer is not invoked; <code>correction_attempts</code> is not incremented. <strong>A rejected <code>report_goal_*</code> call does NOT mirror its <code>status_text</code> onto the card and does NOT update <code>latest_self_report</code>.</strong> Those fields are written only when a <code>report_goal_*</code> call is accepted (or when an executor&#39;s terminal result is accepted). The runtime also emits <code>goal_report_rejected { kind: &#39;invalid_evidence&#39; }</code> for operator visibility.</p><p>Repeated <code>invalid_evidence</code> errors are governed only by the standard stuck-supervisor path and by <code>report_goal_done</code> not consuming review retries; there is no separate cap. The planner may keep retrying as long as the supervisor and runtime pause controls allow.</p><p>There is no notification-acknowledgement gate. Notifications never block <code>report_goal_done</code> and are not part of the activation contract.</p><h3 id="_8-2-runtime-acceptance-gates" tabindex="-1">8.2 Runtime Acceptance Gates <a class="header-anchor" href="#_8-2-runtime-acceptance-gates" aria-label="Permalink to &quot;8.2 Runtime Acceptance Gates&quot;">​</a></h3><p>Before invoking the reviewer, the runtime additionally validates that the goal&#39;s subtree is safe to close:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SubtreeReadinessReason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;descendant_blocking&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;changed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span></code></pre></div><p>The runtime rejects <code>report_goal_done</code> with a <code>tool_error</code> of kind <code>subtree_not_ready</code> whose payload is <code>{ reasons: SubtreeReadinessReason[] }</code> when any descendant card is in status <code>blocked</code> or <code>changed</code>. (Additional statuses can be added to this list in the future without breaking the contract.)</p><p>The gate does not need to scan descendants for live subprocesses: by invariant, a subgoal cannot have reported <code>done</code> while it held live processes (its own readiness gate would have rejected the report). Once the asynchronous-process work lands (§17), an additional <code>pending_subprocess</code> reason will be added here for the goal card&#39;s own processes.</p><p>On rejection, the runtime appends a synthetic note on the goal card naming the offending descendants. The planner stays <code>Running</code>; <code>active_card_run</code> is not cleared; <code>correction_attempts</code> is not incremented. The runtime emits <code>goal_report_rejected { kind: &#39;subtree_not_ready&#39; }</code>.</p><p>Gate order on <code>report_goal_done</code> is:</p><ol><li><code>subtree_not_ready</code> (§8.2).</li><li><code>invalid_evidence</code> (§8.1).</li><li>Reviewer phase (plan §7.4).</li></ol><h2 id="_9-goal-context" tabindex="-1">9. Goal Context <a class="header-anchor" href="#_9-goal-context" aria-label="Permalink to &quot;9. Goal Context&quot;">​</a></h2><p>Goal Context is generated on planner creation and every runtime resume. It is intentionally basic:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;goal&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;project&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  parent_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  depth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  description</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  acceptance</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tags</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  priority</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  depends_on</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  blocks</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  child_card_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  notes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextNote</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_self_report</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_review_result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewAssessment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  correction_attempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  max_review_retries</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  resume_reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;initial&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer_correction&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;analyst_directive&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_changed&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;service_restart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  child_card_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextCardNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalContextNote</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pending_subtree_correction&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      origin_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      issues</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnalystIssue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_changed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      descendant_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">      at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;subtree_not_ready&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reasons</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SubtreeReadinessReason</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reviewer_interrupted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">assessment_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;analyst_note&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;done&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocked&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  assessment_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>latest_self_report</code> is the planner&#39;s most recent terminal report (<code>report_goal_done / _failed / _blocked</code>) for this goal, persisted on the card. There is no separate <code>PlanningResult</code> type.</p><p>Only the directive note kinds shown above are injected into <code>notes</code>; ordinary notes remain available through note tools.</p><p>Every runtime resume appends one synthetic user turn to the planner&#39;s message log containing the freshly rebuilt Goal Context block plus the resume reason. Prior synthetic context turns are kept; compaction handles drift.</p><p>Durable evidence is one of, scoped to descendants of the goal:</p><ul><li>A registered artifact.</li><li>A registered attachment.</li><li>For terminal cards, a non-null <code>result.executor</code> object that validates against the executor result schema for that card type.</li><li>For goal cards, a <code>result.review.result === &#39;pass&#39;</code> assessment.</li></ul><p>Goal cards without a passed review and terminal cards without a registered artifact, attachment, or valid executor result are rejected as evidence per §8.1.</p><h2 id="_10-reviewer" tabindex="-1">10. Reviewer <a class="header-anchor" href="#_10-reviewer" aria-label="Permalink to &quot;10. Reviewer&quot;">​</a></h2><p>The reviewer validates a goal after its planner reports completion and after the runtime acceptance gates (§8.1, §8.2) pass. Reviewer sessions are one-shot and use stable ids:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>reviewer:&lt;goal_card_id&gt;:&lt;assessment_id&gt;</span></span></code></pre></div><p>The runtime preallocates <code>assessment_id</code> before invocation.</p><p>Reviewer result schema:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerIssue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  severity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;info&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;warning&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocker&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  recommendation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pass&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;needs_corrections&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  achieved</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  issues</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReviewerIssue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_card_ids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>On <code>pass</code>, the runtime stores <code>result.review</code>, marks the goal <code>done</code>, clears retry counters, updates <code>latest_self_report</code> and <code>status_text</code> from the accepted <code>report_goal_done</code> call, and returns <code>done</code> from the activation.</p><p>On <code>needs_corrections</code>, the runtime stores <code>result.review</code>, increments <code>correction_attempts</code>, and either resumes the same planner inside the current activation or, after retry exhaustion, records correction notes and returns <code>failed</code> to the parent. The stored <code>result.review</code> is the parent&#39;s authoritative source for the reviewer issues that caused the failure. The runtime does NOT mirror the rejected planner&#39;s <code>status_text</code> onto the card while corrections are pending; it is only written when a <code>report_goal_done</code> is finally accepted.</p><p><strong>Reviewer interrupt recovery.</strong> If a service restart (or supervisor force-cancel) interrupts a reviewer session before <code>result.review</code> is persisted, the runtime sets the goal&#39;s planner session back to <code>Running</code> with <code>resume_reason: &#39;reviewer_interrupted&#39;</code> and Goal Context that includes a <code>reviewer_interrupted</code> synthetic note. The planner inspects its own subtree (the card status, child cards, artifacts) and, if the work is still complete, calls <code>report_goal_done</code> again. The runtime then re-runs the acceptance gates and reviewer with a fresh <code>assessment_id</code>. The interrupted reviewer session is discarded; nothing it produced is persisted.</p><h2 id="_11-analyst-lets-dance-and-the-project-card" tabindex="-1">11. Analyst, <code>lets_dance</code>, and the Project Card <a class="header-anchor" href="#_11-analyst-lets-dance-and-the-project-card" aria-label="Permalink to &quot;11. Analyst, \`lets_dance\`, and the Project Card&quot;">​</a></h2><p>The runtime starts idle. It does not auto-activate the project card on boot. The analyst kicks the system off with:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lets_dance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span></span></code></pre></div><p><code>lets_dance</code> records a <code>lets_dance</code> directive on the project card. The runtime&#39;s safe-tick loop, on noticing that:</p><ul><li>the runtime is not paused,</li><li><code>active_card_run</code> is <code>null</code>,</li><li>no in-flight startup repair is pending, and</li><li>a pending <code>lets_dance</code> (or persisted project-correction) directive exists,</li></ul><p>calls <code>activate_card(projectCardId)</code> itself, which brings the project planner to <code>Running</code>. Once kicked off, the project runs uninterrupted until it reports done, failed, or blocked, or until the analyst intervenes (see below). The operator/analyst recovery loop for project-level failure is deferred to a future stage.</p><p>Analyst correction on a non-project goal records notes only:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mark_goal_needs_corrections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(goalId, issues, note</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span></code></pre></div><p>It records <code>pending_subtree_correction</code> notes on the origin and ancestor goals and may flip the origin card to <code>changed</code>. It does not activate any card. Reactivation is left to the planner: ancestor planners observe the <code>changed</code> state through Goal Context (as <code>subtree_changed</code> notes and updated child statuses) and decide whether to call <code>activate_card</code> on the affected descendant.</p><p>For project-level intervention after kickoff:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mark_project_needs_corrections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(issues, note</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span></code></pre></div><p>writes a directive note on the project card. Like <code>lets_dance</code>, it records intent only; the runtime decides when (and whether) to call <code>activate_card(projectCardId)</code> based on the same safe-tick conditions.</p><p>The analyst always interacts with mutable state through the pattern <strong>pause → mutate → unpause</strong>:</p><ol><li><code>pause_runtime</code> (global gate).</li><li>Write notes, flip statuses (e.g. mark cards <code>changed</code>), record directives, edit card fields.</li><li><code>resume_runtime</code>.</li></ol><p>The active planner (if any) sees the new notes the next time it is admitted to the LLM conversation. Concretely, the runtime queues the notes and injects them as a synthetic user turn at the next point the target planner is about to receive an assistant turn (a freshly landed <code>tool_result</code>, a fresh activation, or a runtime resume). The mechanism is one-shot per note: the synthetic turn is appended once, then the note is removed from the queue. The card-side status (e.g. <code>changed</code>) and any persisted <code>pending_subtree_correction</code> records on the card remain; the queued note is just the delivery vehicle into the LLM conversation.</p><p><strong>Note routing.</strong> A synthetic note (<code>subtree_changed</code>, <code>analyst_note</code>, <code>pending_subtree_correction</code>, <code>subtree_not_ready</code>, <code>reviewer_interrupted</code>) is queued on the session of the deepest planner whose goal subtree contains the affected card. If that planner is currently <code>Dormant</code>, the note remains queued and is delivered the next time the planner is brought to <code>Running</code> by <code>activate_card</code>.</p><p>Guaranteeing semantic consistency of analyst mutations against in-flight runtime state is deferred to a future stage.</p><p>When the project planner is <code>Running</code>, <code>active_card_run</code> points at the project card itself; when it activates a top-level goal, <code>active_card_run</code> moves down to that goal. The runtime does not auto-spawn planners for newly-created goals; every planner, including the project planner, exists because some <code>activate_card</code> call brought it into being.</p><h3 id="_11-1-planner-tool-call-loop-recovery" tabindex="-1">11.1 Planner Tool-Call Loop Recovery <a class="header-anchor" href="#_11-1-planner-tool-call-loop-recovery" aria-label="Permalink to &quot;11.1 Planner Tool-Call Loop Recovery&quot;">​</a></h3><p>Planner turns may use tools, but the final assistant payload delivered to <code>parsePlannerResult</code> must always be the canonical planner JSON envelope. The adapter&#39;s <code>forceFinalAnswer</code> recovery is the documented escape hatch for no-progress tool loops: on a repeated tool-call fingerprint or after the maximum tool rounds, it appends diagnostics to the session, asks the model for a final answer with tools disabled by instruction, and persists/parses only the returned planner envelope. If a planner emits only a deferred <code>activate_card</code>, no follow-up LLM turn is required; the adapter synthesizes <code>{ status: &#39;continue&#39;, summary, created_cards: [], updated_cards: [] }</code> so the parent planner can await the child without leaking an unparseable <code>{ toolCalls: [...] }</code> object into result parsing or runtime state.</p><h2 id="_12-restart-and-orphan-repair" tabindex="-1">12. Restart and Orphan Repair <a class="header-anchor" href="#_12-restart-and-orphan-repair" aria-label="Permalink to &quot;12. Restart and Orphan Repair&quot;">​</a></h2><p>On startup, the runtime repairs orphan tool calls from persisted state:</p><ul><li>If a parent planner has an <code>activate_card</code> tool call with no matching result and the child card has a terminal status on disk, synthesize the terminal <code>CardActivationOutcome</code> and deliver it.</li><li>If <code>active_card_run</code> is persisted and points at a card still mid-flight in <code>phase: &#39;planner&#39;</code>, re-enter that leaf.</li><li>If <code>active_card_run</code> is persisted in <code>phase: &#39;executor&#39;</code> with no persisted executor terminal result, synthesize a <code>failed</code> outcome with <code>failure_kind: &#39;service_restart&#39;</code> and unwind to the parent.</li><li>If <code>active_card_run</code> is persisted in <code>phase: &#39;reviewer&#39;</code> with no persisted <code>result.review</code>, follow the reviewer-interrupt recovery path (§10): transition the goal&#39;s planner session back to <code>Running</code>, set <code>phase: &#39;planner&#39;</code>, and queue a <code>reviewer_interrupted</code> note. The planner is expected to re-issue <code>report_goal_done</code>, and the runtime re-runs the gates and reviewer with a fresh <code>assessment_id</code>.</li><li>Durable async process reattach is current runtime scope: startup reconciles persisted <code>ProcessRecord</code>s with identity probes, reattaches matching live processes where possible, and records mismatches or reattach failures as synthetic lost <code>process_failed</code> terminals.</li></ul><p>Only the owner of the orphaned tool call is resumed. Other planners keep their persisted lifecycle status. After repair, the runtime returns to idle if no <code>active_card_run</code> remains and re-evaluates <code>lets_dance</code> / project-correction directives on its next safe tick.</p><p>Runtime pause is global (<code>RuntimeState.paused</code>). It does not change <code>active_card_run</code>, <code>card.status</code>, or any session&#39;s lifecycle state; it simply stops the runtime from scheduling new LLM turns.</p><p>Force-cancel produces a single synthetic <code>tool_result</code> to the leaf&#39;s parent with <code>failure_kind: &#39;planner_force_cancelled&#39;</code> and clears <code>active_card_run</code>; the parent transitions back to <code>Running</code> on its next turn (which delivers the tool_result).</p><h2 id="_13-configuration" tabindex="-1">13. Configuration <a class="header-anchor" href="#_13-configuration" aria-label="Permalink to &quot;13. Configuration&quot;">​</a></h2><p>Runtime config includes:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RuntimeConfig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  continuousImprovement</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// reserved; see §17</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  maxReviewRetries</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 3</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  processTimeouts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    plannerMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    executorMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    reviewerMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>A goal card may override review retries with:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">card.metadata.max_review_retries</span></span></code></pre></div><p>The effective retry limit is stored in Goal Context as <code>max_review_retries</code>. Semantics: a value of <code>N</code> means the runtime will resume the same planner up to <code>N</code> times after a <code>needs_corrections</code> verdict; the <code>N + 1</code>-th consecutive <code>needs_corrections</code> causes the activation to return <code>failed</code>. <code>correction_attempts</code> is incremented on every persisted <code>needs_corrections</code> verdict and reset to 0 on <code>pass</code>.</p><p>Persisted JSON uses <code>snake_case</code> (<code>max_review_retries</code>, <code>continuous_improvement</code>, <code>planner_ms</code>, etc.); the TypeScript shape above is the in-memory mirror.</p><h2 id="_14-http-api" tabindex="-1">14. HTTP API <a class="header-anchor" href="#_14-http-api" aria-label="Permalink to &quot;14. HTTP API&quot;">​</a></h2><ul><li><p><code>POST /api/runtime/lets_dance</code> — records a <code>lets_dance</code> directive on the project card. Returns <code>{directive_recorded: true, runtime_status: &#39;idle&#39; | &#39;running&#39; | &#39;paused&#39;}</code>. The runtime calls <code>activate_card(projectCardId)</code> itself on its next safe tick.</p></li><li><p><code>POST /api/runtime/goals/:id/needs_corrections</code> — body <code>{issues: AnalystIssue[], note?: string}</code>. The <code>flagged_by</code> field is derived from the authenticated session. Records correction notes for the goal and ancestors and may flip the origin to <code>changed</code>. Resumes no planner and returns no planner session id.</p></li><li><p><code>POST /api/runtime/project/needs_corrections</code> — body <code>{issues: AnalystIssue[], note?: string}</code>. Records a project directive note. The runtime decides on its next safe tick whether to call <code>activate_card(projectCardId)</code>. Returns <code>{directive_recorded: true, runtime_status: &#39;idle&#39; | &#39;running&#39; | &#39;paused&#39;}</code>.</p></li><li><p><code>POST /api/runtime/pause</code> and <code>POST /api/runtime/resume</code> — global pause gate (§5, §12). Returns the updated <code>RuntimeState</code>.</p></li><li><p><code>GET /api/runtime/card-runs</code> — returns a typed union for operator UI:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardRunsResponse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_card_run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActiveCardRun</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  active_breadcrumb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardBreadcrumbNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// server-computed: project -&gt; leaf</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  dormant_planners</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DormantPlannerRow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  cards_with_pending_corrections</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PendingCorrectionRow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardBreadcrumbNode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status_text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DormantPlannerRow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  goal_card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  planner_session_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  latest_self_report</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LatestSelfReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PendingCorrectionRow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  card_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CardStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  note_count</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  last_note_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>The breadcrumb is computed server-side by walking <code>CardStore.getParent</code> from <code>active_card_run.card_id</code> up to the project card; it is not persisted.</p></li><li><p>Legacy dispatch endpoints are removed.</p></li></ul><p><code>AnalystIssue</code> is the single canonical shape for both the analyst tool and the HTTP endpoint:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnalystIssue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  severity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;info&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;warning&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blocker&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  evidence_path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h2 id="_15-invariants" tabindex="-1">15. Invariants <a class="header-anchor" href="#_15-invariants" aria-label="Permalink to &quot;15. Invariants&quot;">​</a></h2><ul><li>At most one planner is <code>Running</code> globally. Ancestor planners are derivably <code>AwaitingChild</code> by virtue of holding an unresolved <code>activate_card</code> tool call in their message log.</li><li><code>active_card_run</code> is either <code>null</code> (idle) or points at the single card-run currently doing work.</li><li>The card hierarchy is the durable source of parent/child structure; the runtime never persists an ancestor chain separately.</li><li>One <code>activate_card</code> tool call yields exactly one terminal tool result.</li><li>The parent sees only <code>done</code>, <code>failed</code>, or <code>blocked</code> from <code>activate_card</code>. Reviewer issues, when relevant, live on the child card&#39;s <code>result.review</code>.</li><li><code>subtree_not_ready</code> and <code>invalid_evidence</code> are <code>report_goal_done</code> tool errors, not reviewer corrections, and do not consume <code>max_review_retries</code>.</li><li><code>status_text</code> is written by the runtime from each agent&#39;s terminal response; no planner tool writes it directly.</li><li><code>AgentSession</code> stores no parent session id and no parent tool call id.</li><li>Runtime caller edges live on <code>active_card_run</code> and process records.</li><li>Planners are created or resumed only inside <code>activate_card</code>, including the project planner; the runtime, not the analyst, calls <code>activate_card(projectCardId)</code> when a <code>lets_dance</code> (or persisted project-correction) directive is pending.</li><li>Runtime pause is global state (<code>RuntimeState.paused</code>); it does not mutate <code>active_card_run</code>, <code>card.status</code>, or any session lifecycle state.</li><li>Re-calling <code>activate_card</code> on a <code>Dormant</code> planner resumes that same planner with the freshly rebuilt Goal Context. The operation is idempotent in the sense that the same planner is reused; it is not side-effect-free, because the planner may emit new tool calls.</li><li>Notifications do not wake planners and do not block <code>report_goal_done</code>.</li><li>Old <code>.saivage/</code> state is not migrated.</li></ul><h3 id="_15-1-planner-tool-authority" tabindex="-1">15.1 Planner Tool Authority <a class="header-anchor" href="#_15-1-planner-tool-authority" aria-label="Permalink to &quot;15.1 Planner Tool Authority&quot;">​</a></h3><p>The planner role is bound by the same write policies as the executor role:</p><ul><li>The write-block list (<code>.saivage/</code>, lockfiles, generated outputs) and secret-path rules apply to <code>write_project_file</code> and <code>run_project_command</code> invoked from any agent role.</li><li>Territory rules from earlier designs (per-card writable subtrees) are advisory in this stage and do not block tool calls; they are recorded for the operator console only.</li><li>The content supervisor inspects every tool output, regardless of role.</li><li>The planner role&#39;s allowed tool set is exactly the list in §7. Any other tool name in a planner turn is a hard tool error.</li></ul><h2 id="_16-file-map" tabindex="-1">16. File Map <a class="header-anchor" href="#_16-file-map" aria-label="Permalink to &quot;16. File Map&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>src/utils/runtime.ts</code></td><td>Runtime bootstrap, <code>lets_dance</code> safe-tick, <code>activate_card</code>, acceptance gates, reviewer loop, single active card-run, restart repair.</td></tr><tr><td><code>src/agents/agent-runtime.ts</code></td><td>Runtime-facing agent interface: activate cards, ensure planners, invoke executors/reviewers, persist verdicts.</td></tr><tr><td><code>src/agents/agent-adapter.ts</code></td><td>Session loop, tool dispatch, single-active-planner enforcement hooks.</td></tr><tr><td><code>src/agents/planner-tools.ts</code></td><td>Planner tool registry: card tools, workspace tools, <code>activate_card</code>, report tools (each carrying <code>status_text</code>).</td></tr><tr><td><code>src/agents/system-prompt.ts</code></td><td>Planner and project-planner prompts.</td></tr><tr><td><code>src/schemas/types.ts</code></td><td><code>RuntimeState</code>, <code>ActiveCardRun</code>, clean session and process schemas.</td></tr><tr><td><code>src/schemas/validators.ts</code></td><td>Validators for clean schemas, reviewer/evidence results, and subtree-readiness reasons.</td></tr><tr><td><code>src/agents/analyst-tools.ts</code></td><td>Analyst correction tools and <code>lets_dance</code> bootstrap.</td></tr><tr><td><code>src/server/server.ts</code></td><td>Runtime correction, <code>lets_dance</code>, pause/resume, and card-run HTTP endpoints.</td></tr></tbody></table><h2 id="_17-future-stages" tabindex="-1">17. Future Stages <a class="header-anchor" href="#_17-future-stages" aria-label="Permalink to &quot;17. Future Stages&quot;">​</a></h2><p>Shipped since this future-stage cycle began: durable process reconciliation now emits the structured restart-time audit events <code>process_reconciled_dead</code> and <code>process_reattach_rejected</code> alongside the existing synthetic terminal records; these events are no longer future-stage work.</p><p>The following items are intentionally out of scope for the current redesign and are listed here so they are not lost:</p><ul><li><strong><code>.saivage/</code> subdirectory layout.</strong> Split persisted state into per-concern subdirectories (sessions, cards, runtime, archive, notifications). The clean-slate boot rule in this design only discards the legacy directory; the new layout is added later.</li><li><strong>Reset-planner feature.</strong> A dedicated operator action to truncate a <code>Dormant</code> planner&#39;s LLM message log so the next <code>activate_card</code> starts with only the Goal Context. Today the operator&#39;s recourse is to <code>add_note</code> and re-activate.</li><li><strong>Continuous improvement agent.</strong> The <code>continuousImprovement</code> config flag is reserved for a future stage where the runtime auto-emits <code>lets_dance</code> (or analogous directives) on a schedule. The field is currently read but has no runtime effect.</li><li><strong>Project-level recovery loop.</strong> Once a project run ends in <code>failed</code> or <code>blocked</code>, today the analyst has to issue a fresh <code>lets_dance</code> after corrections. A dedicated operator/analyst fix-and-resume loop for project-root failures is deferred. The user-facing notification or analyst-handoff side of <code>project_run_completed</code> also lives here.</li><li><strong>Analyst mutation consistency.</strong> Guaranteeing that pause-mutate- unpause mutations leave the active planner&#39;s view consistent with the underlying state across arbitrary edits (status flips, notes, field edits) is deferred. The current contract is best-effort: the planner sees the changes as a synthetic note ASAP and replans.</li><li><strong>Subtree-readiness for descendant subprocesses.</strong> Durable process records now exist and are observable, but the acceptance gate does not yet add a <code>pending_subprocess</code> reason for the goal card&#39;s own processes (descendants remain covered by their own per-goal gates).</li><li><strong>Cancel-cascade through running ancestors.</strong> Cancelling a card whose subtree contains the active leaf would require cascading cancellation through the chain of <code>running</code> ancestors and the active leaf itself. This is deferred; <code>cancel_card</code> currently refuses any target whose subtree contains the active leaf.</li><li><strong>Artifact and attachment registration schemas.</strong> Evidence validation references registered artifacts and attachments (§9). The concrete schemas, on-disk layout, and registration flows are deferred to a dedicated stage.</li></ul><h2 id="_18-validation" tabindex="-1">18. Validation <a class="header-anchor" href="#_18-validation" aria-label="Permalink to &quot;18. Validation&quot;">​</a></h2><p>Documentation changes should pass:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:verify</span></span></code></pre></div><p>Implementation changes should also pass typecheck, build, and focused Jest coverage before broader test runs.</p><h2 id="agent-tool-matrix-source-verified" tabindex="-1">Agent tool matrix (source-verified) <a class="header-anchor" href="#agent-tool-matrix-source-verified" aria-label="Permalink to &quot;Agent tool matrix (source-verified)&quot;">​</a></h2><p><code>npm run docs:verify</code> compares this table with <code>src/agents/agent-adapter.ts</code>, <code>src/agents/workspace-tools.ts</code>, and the analyst tool definitions.</p><table tabindex="0"><thead><tr><th>Role</th><th>Tools</th><th>Code anchor</th></tr></thead><tbody><tr><td><code>analyst</code></td><td><code>diff_card,get_card_history_entry,get_note,lets_dance,list_card_history,list_notes,mark_goal_needs_corrections,mark_note_handled,mark_project_needs_corrections</code></td><td><code>src/agents/agent-adapter.ts:126</code></td></tr><tr><td><code>executor</code></td><td><code>diff_card,get_card_history_entry,get_note,kill_process,list_card_history,list_notes,list_project_files,load_skill,mark_note_handled,mcp_tool_call,read_project_file,run_project_command,start_and_wait,wait_for_process,write_project_file</code></td><td><code>src/agents/agent-adapter.ts:124</code></td></tr><tr><td><code>planner</code></td><td><code>activate_card,add_note,cancel_card,create_card,delete_card,diff_card,edit_card,get_card,get_card_history_entry,get_tree,kill_process,list_card_history,list_cards,list_project_files,read_project_file,report_goal_blocked,report_goal_done,report_goal_failed,restart_card,run_project_command,start_and_wait,wait_for_process,write_project_file</code></td><td><code>src/agents/agent-adapter.ts:56</code></td></tr><tr><td><code>reviewer</code></td><td><code>diff_card,get_card_history_entry,get_note,list_card_history,list_notes,list_project_files,load_skill,mark_note_handled,mcp_tool_call,read_project_file</code></td><td><code>src/agents/agent-adapter.ts:125</code></td></tr></tbody></table>`,157)])])}const g=e(n,[["render",r]]);export{k as __pageData,g as default};

import{_ as a,o as t,c as s,ae as i}from"./chunks/framework.B2ul6ABY.js";const u=JSON.parse('{"title":"Analyst Operator Guide","description":"","frontmatter":{},"headers":[],"relativePath":"analyst.md","filePath":"analyst.md"}'),o={name:"analyst.md"};function n(r,e,l,c,d,h){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="analyst-operator-guide" tabindex="-1">Analyst Operator Guide <a class="header-anchor" href="#analyst-operator-guide" aria-label="Permalink to &quot;Analyst Operator Guide&quot;">​</a></h1><p>Use the analyst as Saivage&#39;s <strong>general operator chat agent</strong>.</p><p>The analyst&#39;s job is three things at once:</p><ul><li><strong>chat partner</strong> — explain what Saivage is doing and answer operator questions in plain language;</li><li><strong>inspector</strong> — read non-secret files, list directories, inspect runtime state, read card/session history, and run bounded inspection shell commands;</li><li><strong>director</strong> — when something needs to change, route the work through cards, notes, or canonical control actions instead of doing the project work directly.</li></ul><h2 id="what-the-analyst-is-for" tabindex="-1">What the analyst is for <a class="header-anchor" href="#what-the-analyst-is-for" aria-label="Permalink to &quot;What the analyst is for&quot;">​</a></h2><p>The analyst is the operator-facing reasoning surface for the whole system.</p><p>Use it to:</p><ul><li>inspect the target project, docs, logs, runtime state, cards, notes, sessions, processes, and audit history;</li><li>explain why work is blocked, stale, failing, or waiting;</li><li>create or amend cards when project work needs to be delegated;</li><li>add directive or escalation notes for running or queued work;</li><li>call canonical controls such as runtime pause/freeze/resume when those controls are the right operator action. Process termination controls are deferred in this cycle.</li></ul><p>Do <strong>not</strong> use the analyst as a substitute executor.</p><p>Project work must still go through the normal owners:</p><ul><li>planner for decomposition and replanning;</li><li>executor for implementation;</li><li>reviewer for acceptance;</li><li>runtime and canonical control services for lifecycle and administrative mutations.</li></ul><h2 id="secret-bearing-path-denylist" tabindex="-1">Secret-bearing path denylist <a class="header-anchor" href="#secret-bearing-path-denylist" aria-label="Permalink to &quot;Secret-bearing path denylist&quot;">​</a></h2><p>Analyst inspection uses one centralized denylist for secret-bearing paths.</p><p>The analyst must not read or expose the contents of paths such as:</p><ul><li><code>.saivage/auth-profiles.json</code></li><li>provider tokens and auth files</li><li><code>.env</code> files and similar env fragments</li><li>SSH keys and key directories</li><li>cloud credential locations</li><li><code>.npmrc</code>, <code>.pypirc</code>, and similar secret-bearing config files</li><li><code>.git/</code> token or credential blobs</li></ul><h3 id="file-reads" tabindex="-1">File reads <a class="header-anchor" href="#file-reads" aria-label="Permalink to &quot;File reads&quot;">​</a></h3><p>If a requested path resolves to a denylisted secret-bearing path, the read is denied before file contents are returned.</p><h3 id="directory-listings" tabindex="-1">Directory listings <a class="header-anchor" href="#directory-listings" aria-label="Permalink to &quot;Directory listings&quot;">​</a></h3><p>Directory listings omit secret-bearing child entries entirely.</p><p>When entries were hidden, the response reports a <code>redacted_count</code> and appends a single <code>&lt;redacted&gt;</code> summary row instead of leaking names. Treat that as an intentional safety boundary.</p><h3 id="shell-commands" tabindex="-1">Shell commands <a class="header-anchor" href="#shell-commands" aria-label="Permalink to &quot;Shell commands&quot;">​</a></h3><p>If a shell command targets a denylisted secret-bearing path, the command is classified as unsafe. On analyst web chat, destructive shell commands return a redacted preview with a <code>preview_hash</code> and do not execute unless the caller re-submits with <code>confirmed: true</code> plus the matching hash under a surface/authz combination that permits preview-only confirmation. On Telegram, <code>run_shell_command</code> remains unavailable.</p><h2 id="shell-command-classification-rules" tabindex="-1">Shell-command classification rules <a class="header-anchor" href="#shell-command-classification-rules" aria-label="Permalink to &quot;Shell-command classification rules&quot;">​</a></h2><p>The analyst can run <strong>bounded inspection shell commands</strong>. Each invocation is dynamically classified before authz is applied.</p><p>Possible classes:</p><ul><li><strong><code>read_only</code></strong> — pure inspection; allowed on web chat; not audited as a mutating control action</li><li><strong><code>low</code></strong> — non-read-only but not obviously destructive; allowed on web chat; audited</li><li><strong><code>destructive</code></strong> — unsafe host mutation or secret-targeting behavior; preview-only on analyst web chat, and unavailable on Telegram</li></ul><h3 id="read-only-examples" tabindex="-1">Read-only examples <a class="header-anchor" href="#read-only-examples" aria-label="Permalink to &quot;Read-only examples&quot;">​</a></h3><p>Examples of <code>read_only</code> inspection commands include:</p><ul><li><code>ls -la</code></li><li><code>cat docs/operator-runbook.md</code></li><li><code>grep -r analyst docs/</code></li><li><code>git status</code></li><li><code>git diff --stat</code></li><li><code>systemctl status saivage</code></li><li><code>journalctl --no-pager -u saivage</code></li><li><code>curl -fsS http://localhost:8080/health</code></li><li><code>node --version</code></li></ul><p>Pipelines or chains remain read-only only when <strong>every</strong> segment is read-only, for example:</p><ul><li><code>ls | grep foo</code></li></ul><h3 id="low-examples" tabindex="-1">Low examples <a class="header-anchor" href="#low-examples" aria-label="Permalink to &quot;Low examples&quot;">​</a></h3><p>Commands that are not on the read-only allowlist and are not obviously destructive are classified <code>low</code>.</p><p>Examples:</p><ul><li><code>python3 scripts/report-state.py</code></li><li><code>node tools/inspect-runtime.js</code></li><li><code>bash -lc &#39;for f in docs/*.md; do echo &quot;$f&quot;; done&#39;</code></li></ul><p>These are still inspection-oriented commands. Their output is bounded, timeout-limited, and redacted before persistence.</p><h3 id="destructive-or-preview-only-examples" tabindex="-1">Destructive or preview-only examples <a class="header-anchor" href="#destructive-or-preview-only-examples" aria-label="Permalink to &quot;Destructive or preview-only examples&quot;">​</a></h3><p>Examples of commands treated as destructive on analyst web chat, so they return a redacted preview/confirmation flow instead of executing immediately:</p><ul><li><code>sudo systemctl restart saivage</code></li><li><code>rm -rf .saivage-work/tmp</code></li><li><code>echo hi &gt; /etc/foo</code></li><li><code>apt-get install ...</code></li><li><code>npm install</code></li><li><code>git reset --hard</code></li><li><code>git push --force</code></li><li><code>cat .saivage/auth-profiles.json</code></li></ul><p>On Telegram, <code>run_shell_command</code> remains unavailable.</p><h2 id="shell-parameter-bounds-and-confirmation-semantics" tabindex="-1">Shell parameter bounds and confirmation semantics <a class="header-anchor" href="#shell-parameter-bounds-and-confirmation-semantics" aria-label="Permalink to &quot;Shell parameter bounds and confirmation semantics&quot;">​</a></h2><p><code>run_shell_command</code> accepts these parameters:</p><ul><li><code>command</code> — required non-empty string</li><li><code>cwd</code> — optional working directory; defaults to the project root</li><li><code>timeoutMs</code> — optional timeout in milliseconds; default <code>15000</code>, clamped to a maximum of <code>60000</code></li><li><code>maxOutputBytes</code> — optional per-stream output cap in bytes; default <code>65536</code>, clamped to a maximum of <code>1048576</code></li><li><code>confirmed</code> — optional boolean used to confirm a preview-only action</li><li><code>preview_hash</code> — optional string returned by the prior preview response</li></ul><p>Current hardening behavior:</p><ul><li>malformed parameter types are rejected before execution;</li><li><code>cwd</code> must stay within the project root;</li><li>secret-bearing <code>cwd</code> paths are rejected before execution;</li><li>preview hashing uses the normalized execution inputs, so over-large timeout/output values cannot bypass confirmation matching;</li><li>stdout/stderr are redacted before return or persistence and may include a <code>[truncated N bytes]</code> footer when capped.</li></ul><h2 id="delegation-invariant" tabindex="-1">Delegation invariant <a class="header-anchor" href="#delegation-invariant" aria-label="Permalink to &quot;Delegation invariant&quot;">​</a></h2><p>The analyst may use shell and filesystem tools to <strong>understand</strong> the system, but not to do project delivery work.</p><p>The analyst must not use shell or other direct tools to:</p><ul><li>edit the target project&#39;s source tree;</li><li>run builds or tests as a substitute for executor or reviewer delivery work;</li><li>deploy or mutate the host outside canonical operator controls;</li><li>overwrite cards, notes, or agent artifacts outside the canonical Saivage services.</li></ul><p>When project work needs to happen, the analyst should:</p><ul><li>create or amend a card;</li><li>add a directive or escalation note;</li><li>call the canonical control that already owns the action.</li></ul><h2 id="persistent-web-chat-panel" tabindex="-1">Persistent web chat panel <a class="header-anchor" href="#persistent-web-chat-panel" aria-label="Permalink to &quot;Persistent web chat panel&quot;">​</a></h2><p>The analyst chat panel is available from <strong>every major workspace view</strong>.</p><p>Operators can open it by:</p><ul><li>using the Analyst button in the workspace header;</li><li>pressing <strong>Ctrl+J</strong> on Windows/Linux;</li><li>pressing <strong>Cmd+J</strong> on macOS.</li></ul><p>The panel stays persistent in the web shell so operators can keep investigating while moving between dashboard, cards, agents, files, and debug views.</p><h2 id="discuss-with-analyst-from-card-detail" tabindex="-1">&quot;Discuss with analyst&quot; from card detail <a class="header-anchor" href="#discuss-with-analyst-from-card-detail" aria-label="Permalink to &quot;&quot;Discuss with analyst&quot; from card detail&quot;">​</a></h2><p>Card detail provides a <strong>Discuss with analyst</strong> entry point.</p><p>Use it when a specific card needs explanation, triage, or follow-up delegation.</p><p>That action seeds the analyst conversation with card context so the new chat starts already grounded in the selected card. The seeded context is for inspection and reasoning; if the card needs changes, the analyst still routes those changes through card edits, notes, or canonical controls.</p><h2 id="live-attribution-and-transcript-behavior" tabindex="-1">Live attribution and transcript behavior <a class="header-anchor" href="#live-attribution-and-transcript-behavior" aria-label="Permalink to &quot;Live attribution and transcript behavior&quot;">​</a></h2><p>Analyst-driven mutations must surface back into the operator UI quickly and with attribution.</p><h3 id="card-history-chips" tabindex="-1">Card history chips <a class="header-anchor" href="#card-history-chips" aria-label="Permalink to &quot;Card history chips&quot;">​</a></h3><p>When the analyst changes a card through canonical card mutation, the card&#39;s history view shows that the change came from an analyst chat turn.</p><h3 id="analyst-tool-invoked-transcript-chips" tabindex="-1"><code>analyst_tool_invoked</code> transcript chips <a class="header-anchor" href="#analyst-tool-invoked-transcript-chips" aria-label="Permalink to &quot;\`analyst_tool_invoked\` transcript chips&quot;">​</a></h3><p>When the analyst invokes a visible operator-facing tool action, the transcript can show an <code>analyst_tool_invoked</code> chip so operators can see that the current state change was produced by the analyst&#39;s tool call rather than a manual page refresh or unrelated background event.</p><p>The <code>analyst_tool_invoked</code> broadcast is sanitized before emission. Operators should expect concise summaries rather than raw secret-bearing tool payloads.</p><h3 id="toaster-behavior" tabindex="-1">Toaster behavior <a class="header-anchor" href="#toaster-behavior" aria-label="Permalink to &quot;Toaster behavior&quot;">​</a></h3><p>The app shell shows a small analyst-action toaster for live analyst mutations and related broadcasts. Use it as a freshness signal, then inspect the updated card, history, or notifications view for the authoritative state.</p><h2 id="validation-artifact-recording-convention" tabindex="-1">Validation artifact recording convention <a class="header-anchor" href="#validation-artifact-recording-convention" aria-label="Permalink to &quot;Validation artifact recording convention&quot;">​</a></h2><p>For future Analyst Operator validation or follow-up stages, record validation evidence in the stage report or summary in a consistent, easy-to-audit format.</p><p>Include:</p><ul><li>a <code>commands_run</code> list with the <strong>exact command</strong>, <code>cwd</code>, and pass/fail result for each validation step;</li><li>stable <code>stdout</code>/<code>stderr</code> log paths or artifact paths when logs are saved, preferably under the current stage&#39;s <code>reports/</code> directory or another stage-local path;</li><li>explicit validation outcomes for each required check, not just a generic success summary;</li><li>a note when the worktree had unrelated dirty files so later reviewers can separate pre-existing state from stage changes.</li></ul><p>When practical, prefer stage-local log or artifact paths over ad hoc temporary locations so future audits can find the evidence without re-running validation.</p><h2 id="focused-web-validation-cadence" tabindex="-1">Focused web validation cadence <a class="header-anchor" href="#focused-web-validation-cadence" aria-label="Permalink to &quot;Focused web validation cadence&quot;">​</a></h2><p>Wave L-M analyst web validation runs under <strong>Vitest from <code>/work/saivage-v3/web</code></strong>, not root Jest.</p><p>Use these suites for focused analyst UI regression checks:</p><ul><li><code>src/__tests__/analyst-chat-panel.test.ts</code> — AnalystChatPanel rendering, tool chips, composer, unsaved new-chat behavior</li><li><code>src/__tests__/analyst-chat-store.test.ts</code> — analyst chat Pinia store seeding, synthetic hint draining, local new-chat state</li><li><code>src/__tests__/app-shell-analyst-drawer.test.ts</code> — persistent app-shell drawer entry points and keyboard shortcut</li><li><code>src/__tests__/analyst-toaster.test.ts</code> — live analyst mutation toaster behavior</li><li><code>src/__tests__/card-detail-view.test.ts</code> — card detail live refresh, <code>analyst_tool_invoked</code> reactions, and attribution-adjacent updates</li><li><code>src/__tests__/card-history-panel-analyst-filter.test.ts</code> — analyst-authored card history filtering and attribution copy</li><li><code>src/__tests__/ws-store.test.ts</code> — WebSocket store routing, reconnect, stale, and unauthorized behavior used by the analyst surfaces</li></ul><p>Canonical focused command from the web workspace:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /work/saivage-v3/web</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/__tests__/analyst-chat-panel.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/analyst-chat-store.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/app-shell-analyst-drawer.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/analyst-toaster.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/card-detail-view.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/card-history-panel-analyst-filter.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  src/__tests__/ws-store.test.ts</span></span></code></pre></div><p>Optional root convenience wrapper:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> web:test:analyst-ui</span></span></code></pre></div><p>Do <strong>not</strong> try to run these Vue/Pinia suites through root <code>npm test</code>; the root Jest config only targets <code>tests/</code> and is not the Wave L-M analyst web validation path.</p><h2 id="related-operator-docs" tabindex="-1">Related operator docs <a class="header-anchor" href="#related-operator-docs" aria-label="Permalink to &quot;Related operator docs&quot;">​</a></h2><ul><li><a href="/docs/operator-runbook.html">Operator Runbook</a></li><li><a href="/docs/troubleshooting.html">Troubleshooting</a></li></ul>`,85)])])}const m=a(o,[["render",n]]);export{u as __pageData,m as default};

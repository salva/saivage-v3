# t1-redacted-idle-state-observation provenance

- Task: `t1-redacted-idle-state-observation`
- Stage: `repair-scheduler-idle-start-next-work-001`
- Access date: 2026-06-01T04:56:37Z
- Acquisition method: local metadata-only observation script run from `/work/saivage-v3`; no external network downloads.
- Sources observed:
  - `systemctl show` for `saivage-v3.service` and `diedrico.service` using selected non-secret status properties only.
  - `journalctl -u <service> -n 200` reduced immediately to keyword/count metadata and latest timestamp; raw journal lines were not persisted in reports.
  - HTTP metadata checks with `curl -o /dev/null -w` for `http://localhost:8081/health`, `http://localhost:8081/health/ready`, and `http://localhost:5173/`.
  - Filesystem metadata/status-category scans under `/work/diedrico-lessons/.saivage/cards/by-id/`, `/work/diedrico-lessons/.saivage/runtime/events.jsonl`, `/work/diedrico-lessons/.saivage/{reviews,diaries,supervision}/`, `/work/diedrico-lessons/lessons/`, and product documents `pipeline-status.md`, `catalog.md`, `backlog.md`, `curriculum.md`.
- Artifact written: `.saivage/stages/repair-scheduler-idle-start-next-work-001/reports/observation-metadata.json`
- Artifact schema: JSON object with `collected_at`, `baseline_cycle026_completed_at`, `services`, `http`, `cards`, `events`, `product_docs`, `reviews`, `diaries`, `supervision`, `lessons`, and `last_lesson_artifact` fields. Card/event/product-file content is represented by statuses, mtimes, counts, sizes, keyword flags, and short checksums only.
- Validation:
  - JSON parsed successfully after writing.
  - HTTP status checks returned 200 for Saivage health, Saivage readiness, and Diedrico root.
  - Card scan counted 7 JSON card files, all status category `done`.
  - Runtime event file existed, parsed as 1288 JSONL lines, and had no timestamped events after the cycle-026 baseline timestamp.
- Redaction/secret policy:
  - No raw HTTP bodies, raw logs, raw event payloads, raw card bodies, environment dumps, auth/provider values, or secret-shaped values are included.
  - Product documents and lesson artifacts were not edited and their bodies were not copied.
- License/terms: local operational/runtime metadata generated by the project; no third-party dataset/license involved.

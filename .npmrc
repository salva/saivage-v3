# npm configuration for saivage-v3

# Save exact versions for reproducible builds
save-exact=true

# Always install devDependencies — overrides NODE_ENV=production behavior
# where npm would silently skip test tooling, TypeScript, linters, etc.
include=dev

Implement the scoped pipeline or data change against the validated schema, then run focused validation on representative inputs. Update `record:///status.md?card=<card-id>` with changed files or artifacts, commands, results, and invariant evidence. Select `done` when the implementation is technically accepted, without implying owner or policy approval; select `implementation_retry` only with changed coding diagnosis or implementation evidence, `schema_revision` when implementation disproves a schema assumption, `blocked` when an actually mandatory decision or input is unavailable, or `failed` for a conclusive failure. Ordinary engineering and accepted graph nodes require no new human signoff.

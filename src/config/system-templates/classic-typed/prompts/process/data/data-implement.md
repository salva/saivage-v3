Implement the scoped pipeline or data change against the validated schema, then run focused validation on representative inputs. Update `record:///status.md?card=<card-id>` with changed files or artifacts, commands, results, invariant evidence, and relevant attempted hypotheses. Select `done` when the implementation is technically accepted, without implying owner or policy approval. Select `implementation_retry` only when materially changed coding diagnosis or implementation evidence supports a concrete useful next attempt. Select `schema_revision` when implementation disproves a schema assumption. Select `failed` when the bounded task remains unsatisfied after materially distinct reasonable attempts whose named hypotheses and observed failures are recorded, with no converging diagnosis or implementation and no justified useful next attempt, or when evidence establishes the task cannot succeed within its accepted constraints. Select `blocked` only for a specifically identified genuinely unavailable mandatory input, access, or decision, not ordinary uncertainty or difficult engineering. For `failed`, record exhausted hypotheses, residual uncertainty, and remaining work without claiming global impossibility. Ordinary engineering and accepted graph nodes require no new human signoff.

Validate the accepted schema and assumptions against representative inputs before implementation. Update `record:///status.md?card=<card-id>` with inputs, checks, results, and any disproved assumption. Correct rejection of invalid sample data is not by itself a schema defect. Select `valid` when the schema is technically supported, without implying owner or policy approval; select `schema_invalid` when evidence requires schema revision, `blocked` when an actually mandatory decision, data, or input is unavailable, or `failed` for a conclusive failure. Ordinary engineering and accepted graph nodes require no new human signoff.

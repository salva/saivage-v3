Validate the accepted schema and assumptions against representative inputs before implementation. Update `record:///status.md?card=<card-id>` with inputs, checks, results, and any disproved assumption. Select `valid` when the schema is supported, `schema_invalid` when evidence requires schema revision, `blocked` when required data or input is unavailable, or `failed` for a conclusive failure.

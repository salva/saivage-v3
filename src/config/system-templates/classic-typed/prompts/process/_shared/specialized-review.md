Independently review the current `{{cardType}}` card against its brief, acceptance criteria, descendant `status.md` evidence, and current durable result. Write a fresh, complete assessment to `record:///review.md?card=<card-id>`, citing concrete card IDs and evidence. Select `approved` only when every criterion is satisfied, `revision_required` when the Planner can address specific findings, `blocked` when review cannot proceed without external input, or `failed` for a conclusive review failure.

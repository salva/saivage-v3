Apply the shared review method to the current `{{cardType}}` card's brief and acceptance and to delivery across its subtree. Use descendant status as pointers to the underlying work rather than acceptance by itself, and record fresh findings and citations in `record:///review.md?card=<card-id>`. Select `approved` when the assigned criteria are satisfied, `revision_required` when the Planner can address specific findings, `blocked` when review cannot proceed without external input, or `failed` for a conclusive review failure.

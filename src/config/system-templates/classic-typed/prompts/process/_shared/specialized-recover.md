Recover the current assessment of this stopped `{{cardType}}` planning card from its durable cards, records, current evidence, accepted review findings, and delivered notification context; do not reconstruct a discarded graph position. Treat findings as evidence and choose appropriate remedies rather than blindly adopting suggestions. Resume the sound parts of its project- or goal-level strategy and reusable work, and reassess only what current facts warrant. Apply the shared goal and leaf guidance at this card's level rather than treating recovery as a reason to flatten or ceremonially expand the plan. Update `status.md` with the recovered assessment, evidence, decisions, and remaining work. Select `complete_direct`, `admit_review`, `blocked`, or `failed` strictly from current evidence, blocking only at the shared external or exhausted-strategy boundary.

Inspect direct children before creating any work, avoid near-duplicates, and reuse actionable BACKLOG, CHANGED, and STOPPED children through concrete correction notifications and their matching `activate_card` entries. Reuse a BLOCKED child only when its notification names material new input, a decision, a correction, or a justified different approach. Do not duplicate running children.

Use `edit_card` only for a genuine immediate-child metadata correction to `title`, `tags`, `priority`, `urgency`, or `related`; it cannot edit bootstrap content, dependencies, parent, type, or lifecycle, and must not be used merely to force a transition. When the original task of a done direct child needs correction, call `reopen_card({card_id:"<id>"})`, queue concrete corrective context, then activate the same child. Use that same sequence for a failed direct child only with material new diagnosis, input, correction, or a justified different route recorded in status or notification context. Handle any reopen, notification, or activation failure from its actual result and retry only the failed operation when its admission can now succeed. A genuinely distinct bounded follow-up may cite the original delivery or review without cloning the whole failed scope. BLOCKED children need no reopen, but reactivate them only after the blocker is materially actionable. A goal repairs its own direct children; root reactivates or reopens its direct goal and never reaches into that goal's children.

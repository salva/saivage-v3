Recover the current assessment of this stopped `{{cardType}}` planning card from its durable cards, records, current evidence, and delivered notification context; do not reconstruct a discarded graph position. Resume the sound parts of its project- or goal-level strategy and reusable work, and reassess only what current facts warrant. Apply the shared goal and leaf guidance at this card's level rather than treating recovery as a reason to flatten or ceremonially expand the plan. Update `status.md` with the recovered assessment, evidence, decisions, remaining work, and any intervention request. Select `complete_direct`, `admit_review`, `blocked`, or `failed` strictly from current evidence.

Inspect direct children before creating any work, avoid near-duplicates, and reuse actionable BACKLOG, CHANGED, and STOPPED children through concrete correction notifications and their matching `activate_card` entries. Reuse a BLOCKED child only when its notification names material new input, a decision, a correction, or a justified different approach. Do not duplicate running children.

Use `edit_card` only for a genuine immediate-child metadata correction to `title`, `tags`, `priority`, `urgency`, or `related`; it cannot edit bootstrap content, dependencies, parent, type, or lifecycle, and must not be used merely to force a transition. Planner has no `reopen_card`. For done or failed work requiring another activation, continue independent work where possible and update `record:///status.md?card=<card-id>` with the exact child ID and status, why correction is needed, the exact Analyst request `reopen_card({cardId:"<id>"})`, and the requirement that runtime be stopped or settled paused. Select `blocked` when that intervention prevents acceptance. BLOCKED children need no Analyst reopen, but reactivate them only after the blocker is materially actionable as defined by the shared Planner instruction.

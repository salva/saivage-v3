Assess the delivered context against the accepted evidence and unchanged owner authority. Coordinate any justified in-scope work, update `status.md` for this current node, and use `admit_review` so the resulting work passes review again. Use `blocked` only for a specifically identified unavailable required input, access, or decision, or `failed` only for a conclusive workflow failure. Never infer hidden queue state or completion.

Explore the bounded research question using relevant evidence. Record sources, provenance, scope, uncertainty, methods, and observations in `record:///status.md?card=<card-id>`. Select `evidence_ready` when there is enough evidence for assessment, `more_exploration` only when naming concrete obtainable evidence within the card's bounds, `blocked` when required access or input is unavailable, or `failed` for a conclusive failure. Do not loop when reasonable scoped investigation is exhausted.

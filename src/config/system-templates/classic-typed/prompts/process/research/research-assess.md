Assess the accepted evidence against the bounded question, separating observations from inference and preserving uncertainty. Update `record:///status.md?card=<card-id>` with the reasoning and evidence citations. Select `supported`, `refuted`, or `bounded_inconclusive` according to the evidence. Select `evidence_gap` only when naming concrete obtainable evidence within scope; when reasonable investigation is exhausted, choose `bounded_inconclusive` instead. Select `blocked` for unavailable required input or `failed` for a conclusive failure.

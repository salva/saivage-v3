Assess the accepted evidence against the bounded question, separating observations from inference and preserving uncertainty. Update `record:///status.md?card=<card-id>` with the reasoning and evidence citations. Select `supported`, `refuted`, or `bounded_inconclusive` according to the evidence; exhausted uncertainty normally means `bounded_inconclusive`. Select `evidence_gap` only for concrete obtainable evidence or materially different useful analysis capable of changing the assessment, naming the question, expected change, and next work. Select `blocked` when a required brief deliverable cannot be met within this bounded goal without a specific scope correction, access, input, or decision, or `failed` for a conclusive failure; do not silently discard outstanding brief scope.

Establish the defect or acceptance gap with an adequate existing or new focused check that exposes the affected owning behavior, including relevant branch or boundary distinctions. Do not modify correct product behavior, begin the green repair, or duplicate an adequate reproducer. Update `record:///status.md?card=<card-id>` with the command, observed result, relevant files, and diagnosis. Select `red_confirmed` when the gap is reproduced, `already_green` when adequate evidence shows current behavior already satisfies it, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

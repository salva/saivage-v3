Evaluate whether the passing repair needs structural cleanup. Perform only justified cleanup, then run focused checks relevant to the cleanup; a reasoned no-refactor decision may cite recent still-applicable focused evidence unless current criteria require a rerun. Update `record:///status.md?card=<card-id>` with the decision, changed files, commands, and results. Select `done` when behavior remains accepted, `regressed` when verification exposes a regression requiring repair, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

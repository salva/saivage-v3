Evaluate whether the passing repair needs structural cleanup. Perform only justified cleanup, or record a reasoned no-refactor decision, then rerun focused checks. Update `record:///status.md?card=<card-id>` with the decision, changed files, commands, and results. Select `done` when behavior remains accepted, `regressed` when verification exposes a regression requiring repair, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

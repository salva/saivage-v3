Assess the affected suite and whether its coverage is meaningful, stable, and scoped rather than merely green. You may cite immediately preceding same-card focused-suite evidence only where unchanged relevant inputs and applicable coverage establish that part of acceptance. Run all remaining affected or acceptance-required checks, and rerun after relevant changes or when a verification question remains unresolved. Update `record:///status.md?card=<card-id>` with commands, results, evidence provenance, why any reused evidence applies, and acceptance reasoning. This `verify` node alone selects `done` when coverage and suite acceptance are established; select `coverage_gap` when meaningful coverage is still absent, `repair_needed` when verification exposes a real failure, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

Diagnose and repair the observed faulty test or fixture, changing product support only when the brief expressly requires it. Run the focused target and update `record:///status.md?card=<card-id>` with diagnosis, changed files, commands, and results. Select `tests_passing` when the focused tests pass, `still_failing` only with changed diagnosis or implementation evidence for another attempt, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

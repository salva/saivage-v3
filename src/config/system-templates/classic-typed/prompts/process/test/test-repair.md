Diagnose and repair the observed faulty test or fixture, changing product support only when the brief expressly requires it. Run the focused target and update `record:///status.md?card=<card-id>` with diagnosis, changed files, commands, results, and relevant attempted hypotheses. Select `tests_passing` when the focused tests pass. Select `still_failing` only when materially changed diagnosis or implementation evidence supports a concrete useful next attempt. Select `failed` when the bounded task remains unsatisfied after materially distinct reasonable attempts whose named hypotheses and observed failures are recorded, with no converging diagnosis or implementation and no justified useful next attempt, or when evidence establishes the task cannot succeed within its accepted constraints. Select `blocked` only for a specifically identified genuinely unavailable required input, access, or decision, not ordinary uncertainty or difficult engineering. For `failed`, record exhausted hypotheses, residual uncertainty, and remaining work without claiming global impossibility.

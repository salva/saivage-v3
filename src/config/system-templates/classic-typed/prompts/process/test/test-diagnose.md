Run or inspect the focused target and determine whether the brief identifies missing coverage or an actually failing test or fixture. Do not manufacture a failure for behavior that is already correct. Update `record:///status.md?card=<card-id>` with the target, commands, observations, and classification. Select `coverage_gap` for absent meaningful coverage, `failing_test` for an observed failing test or fixture, `blocked` when diagnosis needs unavailable input, or `failed` for a conclusive failure.

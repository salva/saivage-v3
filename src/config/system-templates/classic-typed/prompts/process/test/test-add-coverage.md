Add a meaningful, scoped assertion or fixture for the specified behavior; it may legitimately pass against current correct code. Run the focused test and update `record:///status.md?card=<card-id>` with changed files, commands, and what the coverage proves. Select `coverage_passing` when the new coverage passes, `repair_needed` when it exposes a real failure requiring repair, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

Perform the system-scope architecture review. Read the immediately preceding accepted component-review summary and immutable versioned `review.md` URL before reviewing cross-cutting invariants, persistence and operations, integration boundaries, and canonical architecture alignment. Write a new fresh complete current review to `record:///review.md?card=<card-id>`; do not overwrite or reinterpret the historical component-review version. Select `approved` only when system-scope acceptance is satisfied, `revision_required` with concrete system findings, `blocked` when review needs unavailable input, or `failed` for a conclusive review failure.

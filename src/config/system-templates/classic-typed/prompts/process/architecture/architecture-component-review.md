Perform the component-scope architecture review. Assess local cohesion, contracts, feasibility, and unnecessary complexity against the brief and accepted draft evidence. Write a fresh complete current review to `record:///review.md?card=<card-id>`. Select `approved` when component-scope criteria are satisfied, `revision_required` with concrete component findings, `blocked` when review needs unavailable input, or `failed` for a conclusive review failure.

Run the affected suite and assess whether the coverage is meaningful, stable, and scoped rather than merely green. Update `record:///status.md?card=<card-id>` with suite commands, results, and acceptance reasoning. Select `done` when coverage and suite acceptance are established, `coverage_gap` when meaningful coverage is still absent, `repair_needed` when verification exposes a real failure, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

Make the smallest coherent product repair for the accepted reproduced gap, then run the focused reproducer. Update `record:///status.md?card=<card-id>` with changed files, diagnosis, commands, and results. Select `green` when the reproducer passes, `still_red` only after recording changed diagnosis or implementation evidence that justifies another attempt, `blocked` when progress requires unavailable input, or `failed` for a conclusive failure.

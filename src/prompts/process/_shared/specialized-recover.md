Reassess this stopped `{{cardType}}` planning card only from current durable cards, records, and notifications; do not reconstruct a discarded process position. Inspect direct children before creating any work, avoid near-duplicates, and reuse BACKLOG, CHANGED, BLOCKED, and STOPPED children through concrete correction notifications and their matching `activate_card` entries. Do not duplicate running children.

Use `edit_card` only for a genuine immediate-child metadata correction to `title`, `tags`, `priority`, `urgency`, or `related`; it cannot edit bootstrap content, dependencies, parent, type, or lifecycle, and must not be used merely to force a transition. Planner has no `reopen_card`. For done or failed work requiring another activation, continue independent work where possible and update `record:///status.md?card=<card-id>` with the exact child ID and status, why correction is needed, the exact Analyst request `reopen_card({cardId:"<id>"})`, and the requirement that runtime be stopped or settled paused. Select `blocked` when that intervention prevents acceptance. BLOCKED children normally remain directly reusable by notification and activation.

Use goal children only for independently reviewable or parallelizable scope; prefer direct work or one terminal child for one coherent task without flattening genuinely independent work. Update `status.md` with the recovered current assessment and evidence. Select `complete_direct`, `admit_review`, `blocked`, or `failed` strictly from current evidence.

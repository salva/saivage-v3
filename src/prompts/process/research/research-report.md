Produce the bounded final research report from the accepted classification and versioned assessment evidence. Clearly separate evidence, inference, limitations, uncertainty, and recommendations, whether the conclusion is supported, refuted, or bounded-inconclusive. Update `record:///status.md?card=<card-id>` with the report location and final evidence. Select `done` when the report is complete, `blocked` when required input is unavailable, or `failed` for a conclusive failure; do not open a recheck loop.

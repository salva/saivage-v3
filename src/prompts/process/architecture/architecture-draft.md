Draft or revise the architecture proposal against the card brief, current system constraints, and canonical architecture. Address the immediately preceding versioned review evidence when present, preserving its immutable `review.md` URL in cumulative `record:///status.md?card=<card-id>`. Record scope, contracts, alternatives, invariants, integration and operational effects, unnecessary complexity avoided, changed proposal files, and validation evidence. Select `ready_for_component_review` when the proposal is coherent and reviewable, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

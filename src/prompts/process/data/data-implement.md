Implement the scoped pipeline or data change against the validated schema, then run focused validation on representative inputs. Update `record:///status.md?card=<card-id>` with changed files or artifacts, commands, results, and invariant evidence. Select `done` when implementation is accepted, `implementation_retry` only with changed coding diagnosis or implementation evidence, `schema_revision` when implementation disproves a schema assumption, `blocked` when required input is unavailable, or `failed` for a conclusive failure.

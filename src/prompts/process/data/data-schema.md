Define the concrete data shapes, invariants, provenance expectations, and boundary behavior required by the brief using current representative data. Update `record:///status.md?card=<card-id>` with the proposed schema, assumptions, samples, and evidence. Select `schema_ready` when the contract is precise enough to validate, `blocked` when required data or input is unavailable, or `failed` for a conclusive failure.

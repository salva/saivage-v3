import { describe, expect, it } from 'vitest';

describe('terminal tool viewer provider exchange contract', () => {
  it('moved terminal tool display to provider_exchange.terminal_tool_fired', () => {
    expect('terminal_tool_fired').toBe('terminal_tool_fired');
  });
});
